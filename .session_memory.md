# 会话记忆：core-agent 能力边界与信息定价场景讨论

> 生成时间：2026-07-16
> 下次启动建议：优先读本文件 + `.context_memory.md`，然后决定是继续 core-agent 阶段四，还是展开信息定价 PoC。

---

## 一、本次会话前情

- 上一会话因异常中断，工作区残留大量未提交代码。
- 本次会话首先恢复上下文，运行 `mvn clean test` 验证通过（81 tests, 0 failures, 0 errors）。
- 完成两次 git commit 并推送到 `main`：
  - `383e261` `feat(core-agent): 阶段二企业级单 Agent + 阶段三多 Agent 协同中台`
  - `3d1ea29` `docs: 更新上下文记忆、Memory 设计与阶段面试准备文档`
- 推送目标：`https://github.com/cccvip/rag.git`，`main` 分支已同步。

---

## 二、新话题：信息定价分析场景

用户提出一个练手/作品级场景：

> 以足球比赛（英法季军赛）为 case，拉 Polymarket 价格曲线，同时抓微博/懂球帝/X 的声量与情感曲线，用 embedding 聚类 + BERT 情感分析，标注轮换、金靴等事件点，观察两条曲线谁领先、背离多久收敛。

核心目的不是预测比分赚钱，而是理解**信息如何在市场中被定价**，该能力可平移到股票、加密、政策事件等"舆论+资金"双轨场景。

---

## 三、关键结论：core-agent 能否支撑

**结论：能支撑，但 core-agent 应作为编排中台，而不是数据采集/数值计算/可视化层。**

### 3.1 core-agent 能直接提供的

| 能力 | 对应实现 |
|:---|:---|
| 任务分解 | `SupervisorStrategy` + Workers |
| 工具调用 | `ToolRegistry` / MCP Gateway |
| 风险/权限控制 | `GuardRail` 风险分级 |
| 多 Agent 上下文共享 | `SharedMemoryManager` + `MemoryScope` |
| 长任务暂停/恢复 | `Checkpoint` + HITL |
| 背景知识检索 | `VectorRetrieverTool` + RAG |
| 结果评估 | `RagasEvaluator` |
| 对外入口 | `AgentExecutionController.run/resume/status` |

### 3.2 core-agent 不具备、需外部补齐的

| 缺口 | 补齐方式 |
|:---|:---|
| 实时/定时数据采集 | Python 爬虫 / Airflow / Crawler Service |
| Embedding / BERT 推理 | Python 微服务（sentence-transformers + transformers）或 Java ONNX/DJL |
| 时间序列数值计算 | Python pandas/numpy/scipy 或 ClickHouse |
| 时序数据存储 | InfluxDB / TimescaleDB / ClickHouse |
| 可视化 UI | Streamlit / React 单独前端 |
| Polymarket 链上/订单簿数据 | Python/Node 服务封装为 MCP Server 或 REST Tool |

### 3.3 推荐架构

```text
前端/调度器  →  core-agent（Supervisor + Workers + SharedMemory + Checkpoint）
                  ↓
        Tool / MCP Server 层
        • Polymarket Data Service（Python）
        • Social Crawler Service（Python）
        • Embedding + BERT Service（Python）
        • TimeSeries Compute Service（Python）
        • VectorStore（Qdrant/Milvus）
```

---

## 四、待明天继续讨论

1. **是否基于当前 core-agent 做一个最小 PoC？**
   - 建议范围：Supervisor 编排 3 个 Worker（市场数据、情感分析、事件标注），输出"价格-情感背离报告"。
2. **技术栈选择**
   - 外部服务用 Python 还是全部用 Java 生态？
   - Embedding/BERT 服务是自部署还是调用 API？
3. **数据合规与来源**
   - Polymarket 数据走公开 API 还是链上？
   - 微博/懂球帝/X 是否用爬虫，还是先用样例/mock 数据？
4. **与 core-agent 阶段四的关系**
   - 这个场景是否就是阶段四中"可视化执行链路""多模型 Provider""真实向量存储"的落地 case？

---

## 五、明天建议打开的文件

- `f:/ai/rag/.context_memory.md`：core-agent 项目整体状态
- `f:/ai/rag/.session_memory.md`：本文件，今日讨论上下文
- `f:/ai/rag/core-agent/plan.md`：阶段四待实现清单
- `f:/ai/rag/coreAgent/Memory.md`：Memory/Checkpoint/HITL 设计细节

---

*保存原因：用户要求将本次对话保存到独立记忆文件，明日继续。*
