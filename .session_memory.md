# 会话记忆：core-agent 能力边界与信息定价场景讨论

> 生成时间：2026-07-16
> 更新于：2026-07-17
> 下次启动建议：优先读本文件 + `.context_memory.md`，然后决定是继续 core-agent 阶段四，还是迭代信息定价 PoC。

---

## 一、本次会话前情

- 上一会话因异常中断，工作区残留大量未提交代码。
- 本次会话首先恢复上下文，运行 `mvn clean test` 验证通过（81 tests, 0 failures, 0 errors）。
- 完成两次 git commit 并推送到 `main`：
  - `383e261` `feat(core-agent): 阶段二企业级单 Agent + 阶段三多 Agent 协同中台`
  - `3d1ea29` `docs: 更新上下文记忆、Memory 设计与阶段面试准备文档`
- 推送目标：`https://github.com/cccvip/rag.git`，`main` 分支已同步。

---

## 二、新话题：信息定价分析场景

用户提出一个练手/作品级场景：

> 以足球比赛（英法季军赛）为 case，拉 Polymarket 价格曲线，同时抓微博/懂球帝/X 的声量和情感曲线，用 embedding 聚类 + BERT 情感分析，标注轮换、金靴等事件点，观察两条曲线谁领先、背离多久收敛。

核心目的不是预测比分赚钱，而是理解**信息如何在市场中被定价**，该能力可平移到股票、加密、政策事件等"舆论+资金"双轨场景。

**关键转折**：用户质疑"双轨方案是否太重"，最终收敛为**以 Polymarket 价格单轨为主、事件归因解释为辅**的轻量 MVP。

---

## 三、关键结论：core-agent 能否支撑

**结论：能支撑，且已落成一个纯 Java PoC。**

core-agent 不是数据采集/数值计算/可视化层，而是**认知层 + 编排层**：

| 能力 | 对应实现 |
|:---|:---|
| 状态图编排 | `AgentGraph` + `AgentNode` |
| 工具调用 | `ToolRegistry` + `Tool` |
| 异常检测 | `Apache Commons Math`（封装为 Tool）|
| 事件归因 | `Spring AI` LLM（封装为 Tool）|
| 报告生成 | Markdown 模板（封装为 Tool）|
| REST 入口 | `InfoPricingController` |

---

## 四、已交付：core-agent-example-info-pricing 模块

### 4.1 提交记录

- `6c1cd5b` `feat(info-pricing): 新增 Polymarket 信息定价分析示例模块（纯 Java）`
- `b63fdfe` `refactor(info-pricing): 统一使用 Gson 替换 Jackson`
- 已推送到 `main`

### 4.2 模块结构

```
core-agent-example-info-pricing/
├── InfoPricingApp.java                         # 启动类
├── application/InfoPricingService.java         # 状态图编排服务
├── application/InfoPricingProperties.java      # 配置属性
├── domain/MarketDataPoint.java                 # 价格数据点
├── domain/AnomalyPoint.java                    # 异常跳变点
├── domain/PricingTimeline.java                 # 报告模型
├── infrastructure/PolymarketDataTool.java      # 价格数据获取
├── infrastructure/AnomalyDetectionTool.java    # 异常检测
├── infrastructure/NewsSearchTool.java          # 新闻搜索 SPI
├── infrastructure/GNewsSearchTool.java         # GNews API 实现
├── infrastructure/MockNewsSearchTool.java      # mock 新闻回退
├── infrastructure/NewsAttributionTool.java     # LLM 事件归因
├── infrastructure/ReportGenerationTool.java  # Markdown 报告
├── interfaces/InfoPricingController.java       # REST 入口
├── interfaces/AnalyzeRequest.java            # 请求 DTO
└── test/InfoPricingServiceTest.java            # 集成测试
```

### 4.3 工作流

```
fetchData → detectAnomalies → searchNews → attributeEvents → generateReport → end
```

新增 `searchNews` 节点：根据价格数据时间窗口，向前后各扩展 6 小时搜索真实新闻。

### 4.4 验证

- `mvn clean test` 全量通过（82 tests，含新增 1 个）
- mock 模式无需真实 LLM API 即可跑通
- REST 入口：`POST /api/info-pricing/analyze`
- 端口：`8085`

### 4.5 当前局限

- 价格数据为 mock（真实 Polymarket API 已接，默认 mock）
- 真实新闻归因已接入 GNews API，但默认使用 mock fallback
- 未做可视化（只输出 Markdown 报告）
- 原目标市场"France vs England 2026"未在 Polymarket 上找到，暂用西班牙冠军市场

---

## 五、2026-07-17 会话新增：真实新闻搜索归因

### 5.1 改动

- 新增 `NewsSearchTool` SPI + `NewsArticle` 模型
- 新增 `GNewsSearchTool`：真实 GNews API 搜索，按 market 时间窗口前后各扩展 6 小时；内置内存级限流与每日配额，超出时自动 fallback 到 mock
- 新增 `MockNewsSearchTool`：内置 mock 新闻列表（硬编码在代码中，不放在 `application.yml`），当 GNews 不可用时回退
- 重构 `NewsAttributionTool`：非 mock 模式用 LLM 对真实新闻做归因；无新闻时 fallback 到 mock 新闻列表
- 状态图增加 `searchNews` 节点：
  `fetchData → detectAnomalies → searchNews → attributeEvents → generateReport → end`
- 新增配置项：
  - `info-pricing.news-search-mock-enabled`（默认 true）
  - `info-pricing.gnews-api-key`（`${GNEWS_API_KEY}`）
  - `info-pricing.gnews-max-results`
  - `info-pricing.gnews-rate-limit-per-minute`（默认 5）
  - `info-pricing.gnews-daily-quota`（默认 100）

- 增强新闻搜索可观测性：
  - `SearchNewsNode` 打印当前工具类型、市场名、搜索关键词、价格时间范围、扩展后的搜索窗口、返回结果数
  - `GNewsSearchTool` 打印配额状态、完整请求 URL（API key 脱敏）、fallback 原因、返回新闻标题
  - `MockNewsSearchTool` 打印 mock 仓库总数、时间窗口匹配数、匹配的新闻标题
- mock 新闻事件时间改为与 mock 价格数据时间对齐（2024-07-13），确保默认测试能匹配到新闻

### 5.2 验证

- `mvn clean test` 全量通过（82 tests，1 skipped）
- mock 模式无需真实 LLM / GNews API 即可跑通
- 真实新闻模式需设置 `GNEWS_API_KEY` 并将 `news-search-mock-enabled` 设为 false
- 配额默认值：每分钟最多 5 次、每日最多 100 次；超出自动 fallback 到 mock，不会中断状态图执行

### 5.3 仍待完成

1. 可视化：价格曲线 + 异常点/新闻标注
2. 替换目标市场：继续寻找 France vs England 2026 或类似真实赛事市场
3. 时序分析：领先/滞后、背离收敛时长

*保存原因：用户要求将本次对话保存到独立记忆文件，明日继续。*

*更新：2026-07-17 完成真实新闻搜索归因。*
