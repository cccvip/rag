# XIAO

1327284XXXX · duibuqiXXXX@gmail.com · 成都 · 9 年经验

---

## 个人简介

9 年 Java 后端经验，近 2 年转型 AI Agent 工程化。核心能力是**架构设计**：从自研流程引擎（DAG+状态机、5000+ TPS），到 RAG 知识库（120 租户、P99 720ms），到 CoreAgent 平台层（Spring AI 之上的业务集成层，工具注册、返回值预处理、安全护栏、租户管控），本质都是同一件事——**复杂任务的调度、编排与容错**。近期聚焦 Agent 工程化落地：MCP 协议集成、Prompt 模板体系、可观测性建设。日常使用 Claude Code 作为主力 AI 编程工具。熟悉 Docker 等云原生知识，具有 Python/Golang 多语言经验。

---

## 技术栈

| 领域 | 技术 |
|:---|:---|
| 语言与框架 | Java、Spring Boot、Spring Cloud、Spring AI、Dubbo、MyBatis |
| Agent 与大模型 | CoreAgent 平台层、ReAct、Function Calling、Multi-Agent 协同、Prompt Engineering、MCP 协议、Spring AI |
| AI 编程工具 | Claude Code（主力开发工具）、Cursor、Copilot，AI 辅助代码生成与 Review |
| RAG 与检索 | Qdrant（Flat/HNSW）、Elasticsearch（BM25）、Hybrid Search（RRF 融合）、HyDE 查询重写、BGE Embedding |
| 中间件 | Redis（分布式锁、令牌桶限流、缓存策略）、Kafka、RabbitMQ、Nacos、ELK |
| 数据库 | MySQL（分表 DBRouter、主键更新 + 乐观锁、索引优化） |
| 基础设施 | Docker、Jenkins CI/CD、Prometheus、Grafana、Netty |
| 架构能力 | DAG/状态机流程编排、CoreAgent 平台层设计、SaaS 多租户隔离、分片 EventLoop 调度、DDD 领域驱动、规则引擎 |
| 高可用 | 限流（Redis 令牌桶 + 本地降级）、熔断（Resilience4j）、优雅关闭、故障自愈 |
| 设计模式 | 工厂模式、策略模式、模板方法模式、组合模式、观察者模式、代理模式 |

---

## 工作经历

### [公司名称] — 技术架构师

`2023.03 – 至今` · 应急安全 TOB 方向 · 主导架构设计，带领 5 人团队协作交付

**应急安全智能平台**

主导设计并落地面向园区、工厂、学校的应急安全管理 SaaS 平台（120+ 活跃租户，含 4 家世界 500 强）。我负责整体架构设计，核心工作三块：**自研流程引擎**处理确定性业务流程，**RAG 知识库**提供智能检索问答，**Agent 能力层**支撑运维自愈和业务智能化。三个模块共享同一套多租户隔离体系和可观测性基础设施。

---

**自研流程引擎 — 确定性业务流程编排引擎**

平台的业务流程基座，处理访客管理、应急演练、审批流转等确定性工作流。采用 DAG 编排 + 状态机驱动，支持条件分支、异步回调、人工审批节点。

- **分片调度 + 节点执行**：参考 Netty EventLoop 设计分片调度器，按 instance_id 哈希分片，多分片并行，天然保证实例级串行化，无需分布式锁，单机支撑 5000+ TPS。业务方通过 SDK 编写 Handler，引擎自动完成任务派发、回调接收、超时检测。
- **五层租户隔离**：入口限流 → 分片调度 → 分层执行（大客户独立 Lane + 中小客户按套餐分组 + 冷租户 LRU 淘汰）→ 实例配额 → 租户熔断。
- **状态持久化**：主键更新同步落盘 + 乐观锁，节点超时告警 + 人工介入，超时率 < 0.1%。
- **成果**：支撑 120+ 租户、TPS 5000+，单租户流量洪峰 10 倍时其他租户无感知，新产品 MVP 交付周期从 6 周压缩至 2 周，助力签约 2 家世界 500 强客户。

---

**应急安全 RAG 知识库 — 多租户检索增强生成系统**

竞品陆续上线了 AI 知识库功能，市场倒逼我们必须补齐这块能力。我主导了 RAG 知识库从 0 到 1 的架构设计与落地，为平台用户提供安全文档的智能检索与问答。核心挑战是在成本约束下，做到安全合规与性能的平衡。

- **双轨检索架构**：Qdrant 管 Dense 向量检索（Flat 索引，零参数零维护），ES 管 BM25 关键词检索，应用层 RRF 融合。单租户 200~2000 份文档，Flat 索引查询延迟 1ms，比 HNSW 省调优人力。
- **多租户隔离**：共享 Collection + Payload 过滤（tenant_id），应用层二次校验，新租户 onboarding 从 2 天压缩至 5 分钟。
- **三层缓存降本**：Query 答案缓存（命中率 55%）→ HyDE 缓存（30%）→ LLM 响应缓存（40%），三层叠加 LLM 调用成本降 82%，平均延迟从 800ms 降至 220ms。
- **Bad Case 治理**：新租户冷启动检索质量差（Faithfulness 0.55），通过租户级同义词库 + 冷启动保护期修复至 0.82；异常流量攻击导致 LLM 费用暴涨 300%，通过租户级 QPS 限流 + 缓存 + Token 配额三层防御解决。
- **成果**：120 个活跃租户（含 4 家世界 500 强），端到端延迟 P99 720ms，Recall@10 97%，精确信息准确率 99.8%，单租户月均基础设施成本 25 元。

---

**CoreAgent 平台层 — 统一 Agent 基础设施**

在 Spring AI 框架之上封装 CoreAgent 平台层，作为公司 Agent 应用的统一基础设施，解决生产环境中"可靠调用 LLM"的问题。平台层提供 7 个核心模块，其中 4 个通过接口开放给业务实现，3 个为纯平台复用。

- **ToolRegistry（工具注册中心）**：业务方实现 `CoreTool` 接口注册工具，平台按场景（ops/rag/data）隔离工具集，同一 Agent 入口可按意图路由到不同场景工具集。工具元数据 `ToolMeta` 承载风险等级、租户可见性、超时配置等平台管控能力。
- **PreProcessor（返回值预处理）**：平台提供路由链 `PreProcessorChain`，业务方实现具体预处理逻辑。运维场景：日志聚合去重（5000 行→20 行摘要）、指标趋势提取；RAG 场景：文档截断、引用提取。避免原始数据撑爆 context window。
- **ContextManager（上下文窗口管理）**：平台提供 Token 计数和溢出裁剪机制，业务方通过 `ContextStrategy` 决定优先级。运维场景：日志指标优先保留；RAG 场景：检索文档优先保留（需要引用溯源）。
- **GuardRail（安全护栏）**：平台提供检查框架、频率限制、人工确认服务，业务方通过 `RiskRule` 定义风险判定。四级分级：LOW 直接执行、MEDIUM 审计日志、HIGH 人工确认、CRITICAL 不开放给 Agent。
- **TenantCtrl（租户管控）**：纯平台组件。Token 月度配额 + QPS 令牌桶限流 + 成本追踪，防止租户间资源抢占。
- **AgentTracer（调用链追踪）**：纯平台组件。记录每步推理的 Thought/Action/Observation，接入 Prometheus + Grafana，成本和延迟可追踪。
- **AgentExecutor（ReAct 推理引擎）**：纯平台组件。串联所有模块，执行 Thought→Action→Observation 循环。

**业务场景接入**：基于统一 Agent 入口，按用户意图路由到不同场景工具集：
- **RAG 问答**：调用安全知识库检索工具，实现 Agent 化检索生成；
- **运维自愈**：调用故障诊断与修复工具，夜间人工介入率从 100% 降至 15%，MTTR 从 45 分钟压缩至 8 分钟；
- **运营数据查询**：调用数据查询工具，运营人员通过自然语言查询到访人数、Token 用量、数据用量，无需写 SQL。

三类场景共享同一套 ToolRegistry、GuardRail、TenantCtrl 和 AgentTracer，新场景只需实现 4 个接口即可接入。

**多步骤协同**：单个复杂任务在 AgentExecutor 内部拆分为 Retriever（检索）、Reranker（精排）、Writer（生成）三个子步骤，串行编排。单步骤 Faithfulness 0.72，三步协同提升至 0.89，延迟增加 50%（300ms→450ms），准确性收益大于延迟代价。

**本地化部署**：基于 Qwen 模型本地部署，数据不出域，满足 TOB 客户安全合规要求。

**技术选型**：选择 Spring AI 而非 LangChain，原因：项目主体是 Java 技术栈，Spring AI 与 Spring Boot 天然集成；本地部署场景下 Java 生态的运维工具链更成熟；团队 5 人均为 Java 背景，Python 框架的学习成本不可忽视。

**MCP 协议接入层**：在 CoreAgent 平台层之上实现 MCP Server 适配，将平台内注册的工具、资源、提示词通过标准 MCP 协议暴露给外部 LLM 客户端（Claude Desktop / Cursor / 企业自研 Agent）。复用 CoreAgent 的 GuardRail、TenantCtrl、AgentTracer 等治理模块，使外部客户端可安全调用企业安全知识库与运维自愈能力，无需改造既有 CoreTool 实现。

---

### 成都海康威视数字技术有限公司 — Java

`2022.04 – 2023.02` · 水利平台研发负责人

**水利平台项目**

平台承接三方水位设备数据，上报实时数据到综合平台，并进行数据清洗再分发到各个下级平台。技术栈：Netty、SpringBoot、MySQL、Redis、Kafka、MyBatis、ES、Flink。

- 重构项目，使用工厂模式、模板模式等设计模式重构代码并梳理逻辑，将单机扩容到 4 台机器
- 引入 Redis 作为缓存，展示最新告警信息及分布式锁使用
- 基于 MyBatis 自写 DBRouter 组件，使用哈希散列实现分表功能
- 使用 Kafka 消费设备上报数据，以及清洗数据下发到各个二级平台
- 利用 Flink 同步数据到 ES，提供数据搜索、自动补全等功能
- **成果**：接入 1000+ 水位设备，每天产生 720W 条数据（约 20G），保证业务正常运行，如期交付

---

### 上海看榜信息科技有限公司成都分公司 — Java

`2017.05 – 2022.03` · 短视频数据平台 · 基础架构 · 运维管理

**营销平台抽奖系统（NX-Lottery）**

NX-Lottery 是新抖数据平台中最重要的微服务之一，主要为了满足年尾、年中等各个重大节日的拉新、促活、留存 C 端用户的抽奖系统。技术栈：SpringBoot、MyBatis、Dubbo、MySQL、Redis。

- 使用 DDD 分层结构搭建整个抽奖系统
- 运用工厂、策略、组合、模板等设计模式抽象业务发奖流程和发奖策略
- 通过组合模式设计适配人群的规则引擎，包括逻辑过滤器和引擎过滤器
- 通过 Kafka 解耦发奖流程；通过 XXL-JOB 任务补偿发奖失败的消息
- 对于活动参与次数的扣减，使用 Redis 分布式锁代替 MySQL 行锁，提供并发能力
- **成果**：抗住秒杀峰值 TPS 3000，支持横向扩容；自研规则引擎完成系统内多个过滤规则的服务要求

---

**API 网关**

以 Netty 组件为基础，采用对象关系映射框架，解析请求协议，处理泛化调用，构建通信协议模型。网关采用分布式方式部署，搭配网关中心统一管理 Nginx 路由，完成网关 SDK 上报的 Dubbo 分配与调用。核心技术：Netty、SpringBoot、Redis、MQ、Dubbo、Nginx。

- 借鉴 MyBatis 的 ORM 模型及数据库连接池实现逻辑，管理连接资源和泛化调用的结果封装，搭建网关通信模型框架
- 横向对比 Zuul 1.0 后选择以 Netty 作为网络层核心框架，完成每日 7000W 的调用，单机最高 7K 并发
- 协作开发网关控制台中心，统一管理应用服务发现；针对异常服务进行熔断与降级处理，并通知对应服务开发者
- 接入第三方阿里云日志服务，使用 MQ 解耦方式完成所有接口调用统计，保障各业务线调用日志查询，每日日志量 30G
- **成果**：项目健康度 99.9%，未发生过因网关服务导致的异常；完成爬虫统计分析（恶意爬虫），维护服务稳定性

---

**运维团队管理**

搭建 Docker 平台，实现全服务上云、应用服务上容器。负责团队管理工作，具备解决非技术类事务的协调能力和沟通能力。

**短视频数据平台核心业务**

负责短视频数据平台的用户增长模块开发，参与设计高并发抽奖系统的限流降级方案，保障年中/年末大促活动期间系统稳定运行。

---

## 教育背景

攀枝花学院 · 软件工程 · 本科 · `2012 – 2016`

---

## 资格证书

计算机二级
