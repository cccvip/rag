# 上下文记忆文档

> 由 Kimi Code CLI 自动生成，记录本次会话的全部关键信息。
> 下次会话启动时，应优先读取此文件以恢复上下文。
>
> **更新说明**：本次会话完成了 `core-agent` 多模块 Maven 重构并推送到 GitHub，
> 同时澄清了阶段一与招聘需求的映射关系、AgentState 不可变设计等关键问题。
> 已替换原有以 RAG 面试稿为主的记忆内容。

---

## 一、用户身份与业务背景

- **角色**：应急安全管理 SaaS 产品的技术负责人
- **团队规模**：6 人，无专职运维
- **当前面试方向**：AI Agent / LLM 应用工程化落地
- **核心项目**：`core-agent`——基于 Spring AI 封装的 Agent 中台服务层
- **近期目标**：把 `core-agent` 从「可运行的 ReAct 原型」升级为「基于状态图的 Agent 编排平台」，
  对齐招聘市场对 Agent 研发的核心要求

---

## 二、项目当前状态（截至本次会话结束）

### 2.1 代码位置

```
f:/ai/rag/core-agent/
├── pom.xml                              # 父 POM
├── core-agent-shared/                   # 共享模型与异常
├── core-agent-api/                      # 公共 API 与 SPI
├── core-agent-runtime/                  # 默认实现（内存版 + MCP Gateway）
├── core-agent-engine/                   # 状态图引擎 + 执行策略 + Agent 入口
└── core-agent-starter/                  # Spring Boot 装配 + 启动类 + REST 接口
```

### 2.2 已完成的阶段一工作

- **ReAct → State Graph 重构**：`Agent.run()` 内部改为 `AgentGraph` + `ReactStrategy` 驱动，对外 API 不变
- **新增状态图核心模型**：`AgentState`、`AgentNode`、`AgentEdge`、`AgentGraph`、`Checkpoint`、`GraphResult`
- **新增策略 SPI**：`ExecutionStrategy`，`ReactStrategy` 为首个实现
- **多模块 Maven 拆分**：5 个子模块，依赖无环
- **依赖解耦**：
  - `AgentGraph<C>` 泛型化，避免 `api` 依赖引擎上下文
  - `McpGateway` 改为接收 `int timeoutMs`
  - `McpToolRegistry` / `StaticServiceResolver` 改为接收简单集合
- **清理**：删除 `target/`、临时脚本 `FixImports*`、`fix_imports.py`，取消跟踪 `.idea/`
- **提交与推送**：
  - `refactor:拆分 core-agent 为五模块 Maven 工程`
  - `docs:更新 plan.md 并添加 VS Code 配置`
  - 因历史提交中存在硬编码 DeepSeek API Key，已用 `git filter-branch` 重写历史并强制推送
  - **提醒**：建议去 DeepSeek 控制台轮换（revoke）该 API Key

### 2.3 构建验证

```bash
cd f:/ai/rag/core-agent
mvn clean test
# Tests run: 42, Failures: 0, Errors: 0, Skipped: 0
# BUILD SUCCESS
```

---

## 三、关键设计决策（需要记住）

### 3.1 为什么用状态图（State Graph）

- 所有执行模式（ReAct、Plan-and-Execute、Multi-Agent）统一建模为有向图
- 天然支持：暂停、恢复、重试、可视化、调试
- 对标 LangGraph，但保留自研可控性

### 3.2 AgentState 为什么设计为不可变

- **状态转移显式化**：每次变更返回新副本，便于追踪
- **Checkpoint / 重放**：一个 `AgentState` 就是完整快照，恢复时直接丢回图里继续跑
- **避免节点间副作用**：节点 A 不会污染节点 B 的输入状态
- **便于测试**：输入确定，输出就确定

> 注意：不是"状态不变"，而是"对象不变，变化通过新对象体现"。

### 3.3 多模块拆分原则

| 模块 | 职责 | 关键类 |
|:---|:---|:---|
| `shared` | 共享枚举/异常 | `RiskLevel`、`McpException` |
| `api` | 接口与 SPI，不依赖 Spring | `AgentGraph`、`AgentState`、`ExecutionStrategy`、`Tool` |
| `runtime` | 可替换的实现 | `McpGateway`、`MetricsTracker`、`InMemoryMemoryManager` |
| `engine` | 编排逻辑 | `Agent`、`ReactStrategy`、`GraphExecutor`、`NodeContext` |
| `starter` | Spring Boot 装配 | `AgentApp`、`McpConfig`、`McpController` |

---

## 四、阶段一对应的招聘需求

阶段一主要覆盖 Agent 工程师 JD 中的以下能力：

1. **Agent 推理引擎实现**：ReAct 多步推理
2. **状态图编排能力**：自研轻量版 LangGraph
3. **策略可插拔架构**：`ExecutionStrategy` SPI
4. **工程化与模块化**：Maven 多模块、 clean architecture
5. **平台级基础设施复用**：GuardRail、TenantCtrl、AgentTracer、MemoryManager

**尚未覆盖**（后续阶段补齐）：

| 能力 | 对应阶段 |
|:---|:---|
| Plan-and-Execute 策略 | 阶段二（已实现） |
| Spring AI Function Calling | 阶段二（已实现） |
| RAGAS / LLM-as-a-Judge 评估框架 | 阶段二（已实现） |
| RAG 检索链路（DocumentStore SPI） | 阶段二（已实现，内存版） |
| 标准 MCP Server | 阶段二（已实现） |
| Reflection 策略 | 阶段三 |
| 并行工具执行 | 阶段三 |
| HITL 人工审批 | 阶段三 |
| 多 Agent 协同（Supervisor + Workers） | 阶段三 |
| A2A 协议 | 阶段三 |
| 持久化 Memory / Checkpoint | 阶段三 |
| 结构化输出（POJO Schema） | 阶段四 |
| 真实 Qdrant / Milvus 向量存储 | 阶段四 |

---

## 五、理解阶段一代码的推荐路径

### 5.1 阅读顺序

1. **`core-agent-api` 模块**：`AgentState` → `AgentNode` → `AgentEdge` → `AgentGraph` → `AgentGraphTest`
2. **`core-agent-engine` 模块**：`ExecutionStrategy` → `ReactStrategy` → `Agent`
3. **`core-agent-runtime` 模块**：`McpGateway`、`MetricsTracker`、`InMemoryMemoryManager`
4. **`core-agent-starter` 模块**：`AgentApp`、`McpConfig`

### 5.2 关键入口

- 状态图执行入口：`AgentGraph.execute(state, ctx)`
- ReAct 编译入口：`ReactStrategy.compile()`
- Agent 调用入口：`Agent.run(sessionId, query)`
- Spring Boot 启动入口：`AgentApp.main()`

### 5.3 调试技巧

在 `AgentGraph.execute()` 的 while 循环里加日志：

```java
log.info("[GRAPH] step={}, node={}, status={}", steps, currentNodeName, state.getStatus());
```

---

## 六、下一步建议

按 `plan.md` 规划，建议进入 **阶段三**：

1. 实现 Supervisor + Workers 多 Agent 架构
2. 接入 A2A 协议
3. 实现 HITL 人工审批节点
4. 实现持久化 Memory / Checkpoint

阶段二已补齐企业级单 Agent 所需的核心能力：Function Calling、评估、MCP、RAG。阶段三是招聘面试中 Multi-Agent 方向的核心考点。

---

## 七、生成的文件与变更

| 文件 | 用途 | 状态 |
|:---|:---|:---|
| `core-agent/pom.xml` | 父 POM，定义 5 子模块 | ✅ 已更新 |
| `core-agent/.gitignore` | 忽略 target/.idea/*.class 等 | ✅ 已新增 |
| `core-agent/core-agent-*/pom.xml` | 各子模块 POM | ✅ 已新增 |
| `core-agent/core-agent-api/...` | 状态图核心模型 | ✅ 已新增/迁移 |
| `core-agent/core-agent-engine/...` | Agent + ReactStrategy + GraphExecutor | ✅ 已新增/迁移 |
| `core-agent/core-agent-runtime/...` | 默认实现 + MCP Gateway | ✅ 已迁移 |
| `core-agent/core-agent-starter/...` | Spring Boot 装配 | ✅ 已迁移 |
| `plan.md` | CoreAgent 2.0 升级计划 | ✅ 已更新 |
| `.vscode/settings.json` | VS Code Java 自动构建配置 | ✅ 已新增 |
| `PlanPromptBuilder.java` | Plan-and-Execute prompt 构建器 SPI | ✅ 已新增 |
| `DefaultPlanPromptBuilder.java` | 通用默认 prompt 实现 | ✅ 已新增 |
| `PlanAndExecuteStrategy.java` | 已移除内置场景 prompt，改为注入 builder | ✅ 已重构 |
| `Agent.java` | 新增 `withStrategy()`，支持策略切换 | ✅ 已重构 |
| `FunctionCallingStrategy.java` | Spring AI Function Calling 策略 | ✅ 已新增 |
| `RagasEvaluator.java` | LLM-as-a-Judge 评估框架 | ✅ 已新增 |
| `McpSseController.java` | 标准 MCP SSE Server | ✅ 已新增 |
| `DocumentStore.java` / `VectorRetrieverTool.java` | RAG SPI 与检索工具 | ✅ 已新增 |
| `PlanAndExecuteIntegrationTest.java` | 真实 LLM 业务场景集成测试 | ✅ 已新增 |
| `coreAgent/Phase1_面试准备.md` | 阶段一面试应答指南 | ✅ 已新增 |
| `coreAgent/Phase2_面试准备.md` | 阶段二面试应答指南 | ✅ 已新增 |
| `core-agent/plan.md` | 项目演进计划 | ✅ 已新增 |
| `.context_memory.md` | 本文件 | ✅ 当前文件 |

---

## 八、高频追问应答索引

| 问题 | 应答位置 |
|:---|:---|
| 阶段一对应哪些招聘需求？ | 本文件第四节 |
| AgentState 为什么不可变？ | 本文件第三节第 2 点 |
| 多模块拆分的原则是什么？ | 本文件第三节第 3 点 |
| 如何理解阶段一代码？ | 本文件第五节 |
| 下一步该做什么？ | 本文件第六节 |

---

*生成时间：2026-07-11*
*下次会话建议：优先读取本文件，然后按需读取 `core-agent/plan.md` 和具体源码文件*
