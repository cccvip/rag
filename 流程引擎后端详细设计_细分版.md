# 自研流程引擎后端详细设计（细分版）

> 输入契约：参考 LogicFlow 的 `nodes + edges` JSON 格式（轻量、可读、易扩展）  
> 设计理念：编译时严格校验、运行时轻量执行、故障时快速恢复  
> 适用场景：技术答辩、架构评审、面试深度追问

---

## 目录

1. [整体架构](#一整体架构)
2. [流程定义层：DSL 输入与编译](#二流程定义层dsl-输入与编译)
3. [运行时核心：状态机引擎](#三运行时核心状态机引擎)
4. [调度与执行层](#四调度与执行层)
5. [持久化与容错](#五持久化与容错)
6. [应急领域特化](#六应急领域特化)
7. [扩展机制](#七扩展机制)
8. [性能与优化](#八性能与优化)
9. [面试速查](#九面试速查)

---

## 一、整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                     外部输入层                               │
│  LogicFlow JSON / 管理后台 API / 版本发布接口                 │
└────────────────────┬────────────────────────────────────────┘
                     │ 发布流程定义
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                    编译与元数据层                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ 定义校验器    │  │ DAG 编译器   │  │ 定义仓库      │      │
│  │ (环/孤立检测)│  │ (JSON→内存)  │  │ (数据库主存)  │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└────────────────────┬────────────────────────────────────────┘
                     │ ProcessDefinition（不可变、运行时编译）
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                    运行时核心层                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ 状态机引擎    │  │ 事件总线      │  │ 上下文管理器  │      │
│  │ (状态转移)   │  │ (内部+外部)  │  │ (变量作用域)  │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└────────────────────┬────────────────────────────────────────┘
                     │ 状态变更事件
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                    调度与执行层                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ 流程驱动器    │  │ 执行器线程池  │  │ 拦截器链      │      │
│  │ (拓扑驱动)   │  │ (同步/异步)  │  │ (AOP 切面)   │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└────────────────────┬────────────────────────────────────────┘
                     │ 持久化指令
                     ▼
┌─────────────────────────────────────────────────────────────┐
│                    基础设施层                                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ MySQL        │  │ Redis        │  │ RocketMQ     │      │
│  │ (状态/定义)  │  │ (缓存/锁)   │  │ (异步/延迟)  │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

**分层原则：**

| 层级 | 职责 | 设计原则 |
|------|------|---------|
| 编译层 | 将外部 JSON 转换为内部可执行模型 | **一次编译，多次运行**；编译时做尽所有校验 |
| 运行时核心 | 驱动状态流转，不碰业务逻辑 | **无锁、单线程调度**；状态转移原子化 |
| 调度执行层 | 决定"什么时候执行什么"，执行业务代码 | **调度与执行分离**；业务代码异步化 |
| 基础设施 | 存储、消息、缓存 | 运行时核心**不直接依赖**基础设施，通过接口抽象 |

---

## 二、流程定义层：DSL 输入与编译

### 2.1 为什么选 LogicFlow 的 JSON 格式作为 DSL？

我们对比过三种输入格式：

| 格式 | 优点 | 缺点 | 结论 |
|------|------|------|------|
| **BPMN 2.0 XML** | 工业标准，开源引擎兼容 | 过于复杂，元素过多，解析慢，应急场景用不到 90% 的规范 | ❌ 过重 |
| **自定义 XML** | 结构严谨 | 前后端解析成本高，扩展需改 Schema | ❌ 不友好 |
| **LogicFlow JSON** | 轻量、前后端同构、人类可读、易扩展 | 非标准，需自行定义执行语义 | ✅ 合适 |

LogicFlow JSON 的核心结构就是 `nodes[] + edges[]`，这与图论中的 DAG 定义天然一致。

### 2.2 输入 JSON 结构

```json
{
  "properties": {
    "processKey": "emergency_dispatch_v2",
    "version": 3,
    "name": "突发事件三级响应流程"
  },
  "nodes": [
    {
      "id": "start_1",
      "type": "start",
      "properties": {}
    },
    {
      "id": "create_incident",
      "type": "service",
      "properties": {
        "handler": "CreateIncidentHandler",
        "timeout": 30,
        "retryTimes": 0
      }
    },
    {
      "id": "notify_sms",
      "type": "service",
      "properties": {
        "handler": "NotifySmsHandler",
        "timeout": 120
      }
    },
    {
      "id": "wait_response",
      "type": "receive",
      "properties": {
        "handler": "WaitResponseHandler",
        "timeout": 300,
        "escalation": [
          { "delay": 300, "action": "SMS", "template": "ESC_L1" },
          { "delay": 600, "action": "PHONE_CALL", "template": "ESC_L2" }
        ]
      }
    },
    {
      "id": "leader_approval",
      "type": "userTask",
      "properties": {
        "executionMode": "MANUAL",
        "assignee": "${initiator.manager}",
        "orphanCheck": {
          "type": "timeout",
          "timeout": "PT48H",
          "escalation": "notifyBoss"
        }
      }
    },
    {
      "id": "auto_archive",
      "type": "service",
      "properties": {
        "executionMode": "AUTO",
        "handler": "ArchiveHandler",
        "timeout": 30,
        "orphanCheck": {
          "type": "heartbeat",
          "interval": "10s",
          "timeout": "60s",
          "action": "retry"
        }
      }
    },
    {
      "id": "end_1",
      "type": "end",
      "properties": {}
    }
  ],
  "edges": [
    { "id": "e1", "sourceNodeId": "start_1", "targetNodeId": "create_incident" },
    { "id": "e2", "sourceNodeId": "create_incident", "targetNodeId": "notify_sms" },
    { "id": "e3", "sourceNodeId": "notify_sms", "targetNodeId": "wait_response" },
    { "id": "e4", "sourceNodeId": "wait_response", "targetNodeId": "leader_approval" },
    { "id": "e5", "sourceNodeId": "leader_approval", "targetNodeId": "auto_archive" },
    { "id": "e6", "sourceNodeId": "auto_archive", "targetNodeId": "end_1", "properties": { "condition": "${allSuccess}" } }
  ]
}
```

**`executionMode` 设计说明：**

| 属性 | 值 | 含义 | 孤儿检测方式 |
|------|-----|------|-------------|
| `executionMode` | `AUTO` | 自动执行节点，引擎提交到线程池执行 | **心跳检测**：后台线程每 10s 写心跳，重启后按心跳超时判定 |
| `executionMode` | `MANUAL` | 人工审批节点，进入 RUNNING 后挂起等待外部操作 | **超时检测**：按业务超时（如 48h）判定，超时后触发升级/转交 |

> 若节点未显式配置 `executionMode`，默认根据 `type` 推断：`service`/`receive` 默认为 `AUTO`；`userTask` 默认为 `MANUAL`。

### 2.3 编译器设计

```java
@Component
public class FlowDefinitionCompiler {
    
    @Autowired
    private NodeTypeRegistry nodeRegistry;
    
    @Autowired
    private ExpressionParser expressionParser;
    
    /**
     * 编译入口：LogicFlow JSON → 内存 ProcessDefinition
     */
    public CompiledProcessDefinition compile(String rawJson) {
        LogicFlowGraph graph = JsonUtils.parse(rawJson, LogicFlowGraph.class);
        
        // 1. 基础结构转换
        ProcessDefinition def = new ProcessDefinition();
        def.setProcessKey(graph.getProperties().getProcessKey());
        def.setVersion(graph.getProperties().getVersion());
        
        // 2. 编译节点
        Map<String, NodeDefinition> nodeMap = new HashMap<>();
        for (LFNode lfNode : graph.getNodes()) {
            NodeDefinition node = compileNode(lfNode);
            nodeMap.put(node.getId(), node);
            def.addNode(node);
        }
        
        // 3. 编译边（构建邻接表和入度表）
        for (LFEdge lfEdge : graph.getEdges()) {
            EdgeDefinition edge = compileEdge(lfEdge);
            def.addEdge(edge);
            
            // 构建运行时所需的邻接表
            def.getAdjacencyList().computeIfAbsent(edge.getSource(), k -> new ArrayList<>()).add(edge);
            def.getInDegreeMap().merge(edge.getTarget(), 1, Integer::sum);
        }
        
        // 4. 严格校验（编译时做尽所有检查）
        validate(def, nodeMap);
        
        return new CompiledProcessDefinition(def, rawJson);
    }
    
    private NodeDefinition compileNode(LFNode lfNode) {
        NodeDefinition node = new NodeDefinition();
        node.setId(lfNode.getId());
        node.setType(lfNode.getType());
        node.setProperties(lfNode.getProperties());
        
        // 校验节点类型是否已注册（后端必须支持该类型的 Behavior）
        if (!nodeRegistry.isRegistered(lfNode.getType())) {
            throw new CompileException("未注册的节点类型: " + lfNode.getType());
        }
        
        return node;
    }
    
    private EdgeDefinition compileEdge(LFEdge lfEdge) {
        EdgeDefinition edge = new EdgeDefinition();
        edge.setId(lfEdge.getId());
        edge.setSource(lfEdge.getSourceNodeId());
        edge.setTarget(lfEdge.getTargetNodeId());
        
        // 解析条件表达式（如 ${allSuccess}）
        String conditionText = lfEdge.getProperties() != null ? lfEdge.getProperties().getCondition() : null;
        if (StringUtils.hasText(conditionText)) {
            edge.setCondition(expressionParser.parse(conditionText));
        }
        
        return edge;
    }
    
    private void validate(ProcessDefinition def, Map<String, NodeDefinition> nodeMap) {
        // 校验 1：有且仅有 1 个 start 节点和至少 1 个 end 节点
        long startCount = def.getNodes().stream().filter(n -> "start".equals(n.getType())).count();
        long endCount = def.getNodes().stream().filter(n -> "end".equals(n.getType())).count();
        if (startCount != 1) throw new CompileException("流程必须有且仅有 1 个开始节点");
        if (endCount < 1) throw new CompileException("流程至少要有 1 个结束节点");
        
        // 校验 2：所有边的 source 和 target 必须存在于 nodes 中
        for (EdgeDefinition edge : def.getEdges()) {
            if (!nodeMap.containsKey(edge.getSource())) {
                throw new CompileException("边引用了不存在的节点: " + edge.getSource());
            }
            if (!nodeMap.containsKey(edge.getTarget())) {
                throw new CompileException("边引用了不存在的节点: " + edge.getTarget());
            }
        }
        
        // 校验 3：DAG 无环检测（DFS）
        if (hasCycle(def)) {
            throw new CompileException("流程定义存在循环依赖，请检查节点连线");
        }
        
        // 校验 4：孤立节点检测（无入边且无出边，或从 start 不可达）
        Set<String> reachableFromStart = bfsReachable(def, getStartNodeId(def));
        for (NodeDefinition node : def.getNodes()) {
            if (!reachableFromStart.contains(node.getId())) {
                throw new CompileException("节点不可达: " + node.getId());
            }
        }
        
        // 校验 5：条件表达式语法检查
        for (EdgeDefinition edge : def.getEdges()) {
            if (edge.getCondition() != null) {
                expressionParser.validate(edge.getCondition());
            }
        }
        
        // 校验 7：executionMode 与 orphanCheck 配置合法性
        validateExecutionModeAndOrphanCheck(def, nodeMap);
    }
    
    private void validateExecutionModeAndOrphanCheck(ProcessDefinition def, Map<String, NodeDefinition> nodeMap) {
        for (NodeDefinition node : def.getNodes()) {
            String executionMode = node.getProperties().getString("executionMode");
            // 未配置则按类型推断
            if (executionMode == null) {
                if ("userTask".equals(node.getType())) {
                    executionMode = "MANUAL";
                } else {
                    executionMode = "AUTO";
                }
            }
            
            JsonNode orphanCheck = node.getProperties().getJsonNode("orphanCheck");
            if (orphanCheck != null) {
                String checkType = orphanCheck.get("type").asText();
                if ("AUTO".equals(executionMode) && !"heartbeat".equals(checkType)) {
                    throw new CompileException("自动执行节点 [" + node.getId() + "] 的 orphanCheck.type 必须是 heartbeat");
                }
                if ("MANUAL".equals(executionMode) && !"timeout".equals(checkType)) {
                    throw new CompileException("人工审批节点 [" + node.getId() + "] 的 orphanCheck.type 必须是 timeout");
                }
            }
        }
    }
    
    private boolean hasCycle(ProcessDefinition def) {
        Set<String> visited = new HashSet<>();
        Set<String> recStack = new HashSet<>();
        
        for (String nodeId : def.getNodeIds()) {
            if (!visited.contains(nodeId)) {
                if (hasCycleDFS(nodeId, visited, recStack, def)) {
                    return true;
                }
            }
        }
        return false;
    }
    
    private boolean hasCycleDFS(String nodeId, Set<String> visited, Set<String> recStack, ProcessDefinition def) {
        visited.add(nodeId);
        recStack.add(nodeId);
        
        List<EdgeDefinition> outgoing = def.getAdjacencyList().getOrDefault(nodeId, Collections.emptyList());
        for (EdgeDefinition edge : outgoing) {
            String neighbor = edge.getTarget();
            if (!visited.contains(neighbor)) {
                if (hasCycleDFS(neighbor, visited, recStack, def)) return true;
            } else if (recStack.contains(neighbor)) {
                return true;
            }
        }
        
        recStack.remove(nodeId);
        return false;
    }
    
}
```

### 2.4 动态加载与场景映射

流程定义**以数据库为唯一主存储**，不依赖启动时全量加载到内存。引擎支持两种启动模式：

1. **固定流程**：直接指定 `processKey` + `version` 启动
2. **动态场景**：传入 `businessCode` + `sceneCode`，由场景映射表解析为具体的流程定义

**典型场景：**
- A 业务：`start("incident_report", 3, variables)` —— 固定流程
- B 业务：`startByScene("SECURITY", "FIRE_ALERT", variables)` —— 动态映射到 `fire_response_v2`

#### 核心模型

```java
public class ProcessDefinitionVersion {
    private String processKey;           // 流程标识，如 "fire_response"
    private Integer version;             // 自增版本号
    private String rawJson;              // 原始 LogicFlow JSON
    private Status status;               // DRAFT / PUBLISHED / DEPRECATED
    private LocalDateTime publishTime;
    private String publishedBy;
}
```

```java
public class FlowSceneMapping {
    private String businessCode;         // 业务编码，如 "SECURITY"
    private String sceneCode;            // 场景编码，如 "FIRE_ALERT"
    private String processKey;           // 映射到的流程标识
    private Integer version;             // 映射到的版本号
    private Status status;               // ENABLED / DISABLED
}
```

#### 流程定义仓库（数据库即主存储）

```java
@Component
public class ProcessDefinitionRepository {
    
    @Autowired
    private FlowDefinitionDao definitionDao;
    
    @Autowired
    private FlowDefinitionCompiler compiler;
    
    /**
     * 从数据库读取 raw_json，实时编译为内存模型
     * 编译结果可缓存，但数据库是唯一可信源
     */
    public ProcessDefinition load(String processKey, Integer version) {
        FlowDefinitionEntity entity = definitionDao.findByKeyAndVersion(processKey, version);
        if (entity == null || entity.getStatus() != Status.PUBLISHED) {
            throw new DefinitionNotFoundException(processKey + ":" + version);
        }
        return compiler.compile(entity.getRawJson()).getDefinition();
    }
    
    public ProcessDefinition loadLatest(String processKey) {
        FlowDefinitionEntity entity = definitionDao.findLatestPublished(processKey);
        if (entity == null) {
            throw new DefinitionNotFoundException(processKey);
        }
        return compiler.compile(entity.getRawJson()).getDefinition();
    }
}
```

#### 场景映射管理

```java
@Component
public class SceneMappingRepository {
    
    @Autowired
    private FlowSceneMappingDao mappingDao;
    
    /**
     * 根据业务编码 + 场景编码，解析出对应的流程定义
     */
    public SceneMappingResult resolve(String businessCode, String sceneCode) {
        FlowSceneMapping mapping = mappingDao.findByBusinessAndScene(businessCode, sceneCode);
        if (mapping == null || mapping.getStatus() != Status.ENABLED) {
            throw new SceneMappingNotFoundException(businessCode + "/" + sceneCode);
        }
        return new SceneMappingResult(mapping.getProcessKey(), mapping.getVersion());
    }
}
```

#### 可选的本地二级缓存

```java
@Component
public class ProcessDefinitionCache {
    
    @Autowired
    private ProcessDefinitionRepository definitionRepository;
    
    // 本地缓存仅作为性能加速层，非必需
    private final Map<String, ProcessDefinition> cache = new ConcurrentHashMap<>();
    
    public ProcessDefinition get(String processKey, Integer version) {
        String key = processKey + ":" + version;
        return cache.computeIfAbsent(key, k -> 
            definitionRepository.load(processKey, version)
        );
    }
    
    // 发布或回滚时失效缓存
    public void invalidate(String processKey, Integer version) {
        cache.remove(processKey + ":" + version);
    }
}
```

**版本策略：**

| 场景 | 行为 |
|------|------|
| 编辑保存 | 生成新的 `DRAFT` 版本，存入数据库 |
| 发布 | DRAFT → PUBLISHED，可选刷新本地缓存 |
| 启动流程实例 | 实时从数据库加载（或走本地缓存），**必须校验 PUBLISHED 状态** |
| 已运行的实例 | **绑定到启动时的 version**，不受后续发布影响 |
| 场景映射变更 | 修改 `flow_scene_mapping` 指向新流程版本，新实例即时生效 |
| 回滚 | 旧版本重新标记为 PUBLISHED，新版本标记为 DEPRECATED，失效缓存 |

**核心约束：版本间不兼容，每次修改均为破坏性变更**

本引擎**不支持**同一 `processKey` 下版本间的向后兼容或平滑升级。设计原则如下：

1. **DSL 是完整流程图**：修改节点拓扑（增删节点、改连线）是常见操作，不存在语义化的"补丁"概念，任何修改都是全新流程
2. **实例强绑定版本**：流程实例启动时锁定 `processKey + version`，运行时从数据库加载对应版本的 raw_json 编译执行，与后续发布完全隔离
3. **升级即替换**：业务升级通过修改 `flow_scene_mapping` 指向新版本实现，老实例继续用老版本跑完，新实例走新版本
4. **简化心智负担**：不需要维护版本间差异、兼容性校验、实例迁移逻辑，降低引擎复杂度

#### 历史数据与版本追溯

版本升级不会影响已产生的历史数据，设计保障如下：

| 数据 | 归属 | 生命周期 | 版本升级时的处理 |
|------|------|---------|----------------|
| 流程实例 (`flow_instance`) | 绑定到 `processKey + version` | 永久保留 | **不删除**，按 `processKey + version` 可查 |
| 活动实例 (`flow_activity`) | 归属到 `instanceId` | 永久保留 | **不删除**，可完整回放节点执行轨迹 |
| 事件日志 (`flow_event_log`) | 归属到 `instanceId` | 永久保留 | **不删除**，审计与对账的依据 |
| 流程定义 (`flow_definition`) | 按 `processKey + version` 存储 | 永久保留 | **不物理删除**，标记为 `DEPRECATED` 仅禁止启动新实例 |
| Token 记录 (`flow_token`) | 归属到 `instanceId` | 流程结束后可清理 | 流程终态后可异步归档或删除 |

**版本追溯示例：**

```java
// 查询半年前的某个历史实例
ProcessInstance instance = instanceRepository.findById("INST_20240115_001");
// instance.getVersion() == 2

// 加载该实例启动时的流程定义（即使版本2已标记为 DEPRECATED）
ProcessDefinition def = definitionRepository.load(
    instance.getProcessKey(), 
    instance.getVersion()  // 指定版本加载，不受当前最新版本影响
);

// 可完整还原当时的流程拓扑和节点配置
List<NodeDefinition> nodes = def.getNodes();
```

**DEPRECATED 的含义：**
- ❌ 禁止启动新的流程实例
- ✅ 正在运行的实例不受影响，继续执行
- ✅ 历史实例的查询、审计、回放不受影响
- ✅ `raw_json` 永久保留在数据库中，支持历史追溯

---

## 三、运行时核心：状态机引擎

### 3.1 流程实例生命周期

```
CREATED → RUNNING → [COMPLETED / TERMINATED / FAILED]
```

- **CREATED**：实例刚创建，尚未开始执行
- **RUNNING**：至少有一个节点处于非终态
- **COMPLETED**：所有路径都到达 end 节点，正常结束
- **TERMINATED**：被外部强制终止
- **FAILED**：关键节点失败且无降级路径，流程异常结束

### 3.2 活动实例状态机

每个节点实例（ActivityInstance）有独立的状态机，这是引擎的核心。

#### 状态定义

```java
public enum ActivityState {
    PENDING,      // 等待上游节点完成，尚未激活
    RUNNING,      // 已激活，正在执行或等待执行
    COMPLETED,    // 执行成功完成
    FAILED,       // 执行失败（可进入重试或降级分支）
    SKIPPED,      // 被条件分支跳过，或被人为跳过
    CANCELLED,    // 流程终止时连带取消
    TERMINATED    // 被人为强制终止
}
```

#### 事件定义

```java
public enum StateEvent {
    START,           // 从 PENDING 激活
    COMPLETE,        // 执行成功
    FAIL,            // 执行异常
    TIMEOUT,         // 执行超时
    RETRY,           // 手动或自动重试
    SKIP,            // 跳过该节点
    CANCEL,          // 流程级取消
    TERMINATE        // 强制终止
}
```

#### 状态转移矩阵

| 当前状态 \ 事件 | START | COMPLETE | FAIL | TIMEOUT | RETRY | SKIP | CANCEL | TERMINATE |
|--------------|-------|----------|------|---------|-------|------|--------|-----------|
| **PENDING** | RUNNING | — | — | — | — | SKIPPED | CANCELLED | TERMINATED |
| **RUNNING** | — | COMPLETED | FAILED | FAILED | RUNNING | — | CANCELLED | TERMINATED |
| **FAILED** | — | — | — | — | RUNNING | — | CANCELLED | TERMINATED |
| **COMPLETED** | — | — | — | — | — | — | CANCELLED | — |
| **SKIPPED** | — | — | — | — | — | — | CANCELLED | — |

> 注：COMPLETED 不接受 TERMINATE，是因为已完成的节点不应被强制回滚，避免下游已执行节点产生不一致。

#### 状态机实现

```java
@Component
public class ActivityStateMachine {
    
    private static final Map<ActivityState, Map<StateEvent, ActivityState>> TRANSITION_TABLE;
    
    static {
        Map<ActivityState, Map<StateEvent, ActivityState>> table = new EnumMap<>(ActivityState.class);
        
        table.put(PENDING, Map.of(
            START, RUNNING,
            SKIP, SKIPPED,
            CANCEL, CANCELLED,
            TERMINATE, TERMINATED
        ));
        
        table.put(RUNNING, Map.of(
            COMPLETE, COMPLETED,
            FAIL, FAILED,
            TIMEOUT, FAILED,
            RETRY, RUNNING,
            CANCEL, CANCELLED,
            TERMINATE, TERMINATED
        ));
        
        table.put(FAILED, Map.of(
            RETRY, RUNNING,
            CANCEL, CANCELLED,
            TERMINATE, TERMINATED
        ));
        
        table.put(COMPLETED, Map.of(
            CANCEL, CANCELLED
        ));
        
        table.put(SKIPPED, Map.of(
            CANCEL, CANCELLED
        ));
        
        TRANSITION_TABLE = Collections.unmodifiableMap(table);
    }
    
    public StateTransitionResult fire(ActivityInstance instance, StateEvent event, Object payload) {
        ActivityState current = instance.getState();
        Map<StateEvent, ActivityState> transitions = TRANSITION_TABLE.get(current);
        
        if (transitions == null || !transitions.containsKey(event)) {
            throw new IllegalStateTransitionException(
                String.format("非法状态转移: %s → 事件[%s]", current, event)
            );
        }
        
        ActivityState next = transitions.get(event);
        
        // 持久化（同步，保证不丢）
        instance.setState(next);
        instance.setUpdateTime(LocalDateTime.now());
        activityRepository.updateState(instance);
        
        // 发布状态变更事件（驱动后续逻辑）
        return new StateTransitionResult(instance.getId(), current, next, event, payload);
    }
}
```

### 3.3 事件总线

状态机只负责"状态转移的原子性"，状态转移后产生的事件通过事件总线分发，解耦状态机与后续动作。

```java
public interface EventBus {
    void publish(DomainEvent event);
    void subscribe(String eventType, EventHandler handler);
}

// 内部实现（进程内）
@Component
public class InMemoryEventBus implements EventBus {
    private final Map<String, List<EventHandler>> handlers = new ConcurrentHashMap<>();
    private final Executor executor = Executors.newCachedThreadPool();
    
    @Override
    public void publish(DomainEvent event) {
        List<EventHandler> subs = handlers.getOrDefault(event.getType(), Collections.emptyList());
        for (EventHandler handler : subs) {
            executor.execute(() -> {
                try {
                    handler.handle(event);
                } catch (Exception e) {
                    // 事件消费异常不影响主流程，记录日志后续处理
                    log.error("事件消费异常", e);
                }
            });
        }
    }
}
```

**订阅方：**

```java
@Component
public class StateTransitionEventRouter {
    
    @Autowired
    private FlowDriver flowDriver;
    
    @Autowired
    private EscalationManager escalationManager;
    
    @Autowired
    private ExternalEventPublisher externalPublisher;
    
    @PostConstruct
    public void init() {
        eventBus.subscribe("ACTIVITY_STATE_CHANGED", event -> {
            StateTransitionResult result = (StateTransitionResult) event.getPayload();
            
            switch (result.getNewState()) {
                case RUNNING:
                    // 节点开始执行，注册超时监听
                    escalationManager.onActivityStarted(result.getActivityId());
                    break;
                case COMPLETED:
                case FAILED:
                case SKIPPED:
                    // 节点到达终态，通知流程驱动器处理下游
                    flowDriver.onActivityFinished(result);
                    // 取消超时监听
                    escalationManager.onActivityFinished(result.getActivityId());
                    break;
                default:
                    break;
            }
            
            // 异步通知外部系统（如前端监控台）
            externalPublisher.publish(result);
        });
    }
}
```

### 3.4 上下文与变量作用域

```java
public class ExecutionContext {
    private String instanceId;
    private String processKey;
    private Integer version;
    private String currentActivityId;
    
    // 流程级变量（整个实例共享）
    private Map<String, Object> processVariables;
    
    // 节点级变量（仅当前节点可见，执行完成后可合并到流程级）
    private Map<String, Object> localVariables;
    
    // 扩展属性（拦截器可读写）
    private Map<String, Object> extensions;
}
```

**变量存取规则：**

| 操作 | 行为 |
|------|------|
| `getVariable("key")` | 先查 local，再查 process |
| `setVariable("key", value)` | 默认写入 process 级，所有后续节点可见 |
| `setLocalVariable("key", value)` | 仅当前节点及同分支下游可见 |
| `getVariable("${expr}")` | 解析 SpEL / Aviator 表达式，支持 `user.name`、`list[0]` 等 |

---

## 四、调度与执行层

### 4.1 Token 机制详解

Token 是流程执行的"控制权令牌"，流程驱动器通过 Token 的位置来决定"现在该执行哪个节点"。

#### Token 数据结构

```java
public class Token {
    private String tokenId;           // 全局唯一
    private String instanceId;        // 所属流程实例
    private String currentNodeId;     // Token 当前所在的节点
    private TokenType type;           // NORMAL / SUB_PROCESS
    private boolean active;           // 是否有效（节点完成后标记为失效）
    private LocalDateTime createdAt;
}
```

#### 启动流程：生成初始 Token

```java
public class ProcessInstanceStarter {
    
    @Autowired
    private ProcessDefinitionRepository definitionRepository;
    
    @Autowired
    private SceneMappingRepository sceneMappingRepository;
    
    /**
     * 固定流程启动：直接指定 processKey + version
     */
    public ProcessInstance start(String processKey, Integer version, 
                                  Map<String, Object> variables) {
        ProcessDefinition def = definitionRepository.load(processKey, version);
        return doStart(processKey, def.getVersion(), def, variables);
    }
    
    /**
     * 固定流程启动（使用最新发布版本）
     */
    public ProcessInstance start(String processKey, Map<String, Object> variables) {
        ProcessDefinition def = definitionRepository.loadLatest(processKey);
        return doStart(processKey, def.getVersion(), def, variables);
    }
    
    /**
     * 动态场景启动：根据业务编码 + 场景编码解析流程定义
     * 示例：startByScene("SECURITY", "FIRE_ALERT", variables) 
     *       → 映射到 fire_response_v2
     */
    public ProcessInstance startByScene(String businessCode, String sceneCode,
                                         Map<String, Object> variables) {
        SceneMappingResult mapping = sceneMappingRepository.resolve(businessCode, sceneCode);
        ProcessDefinition def = definitionRepository.load(mapping.getProcessKey(), 
                                                           mapping.getVersion());
        return doStart(mapping.getProcessKey(), mapping.getVersion(), def, variables);
    }
    
    private ProcessInstance doStart(String processKey, Integer version,
                                     ProcessDefinition def, Map<String, Object> variables) {
        // 1. 创建流程实例
        ProcessInstance instance = new ProcessInstance();
        instance.setInstanceId(generateId());
        instance.setProcessKey(processKey);
        instance.setVersion(version);
        instance.setState(InstanceState.RUNNING);
        instance.setVariables(new ConcurrentHashMap<>(variables));
        instanceRepository.insert(instance);
        
        // 2. 找到 start 节点
        String startNodeId = def.getNodes().stream()
            .filter(n -> "start".equals(n.getType()))
            .findFirst()
            .map(NodeDefinition::getId)
            .orElseThrow();
        
        // 3. 创建初始 Token，放入调度队列
        Token initialToken = new Token(instance.getInstanceId(), startNodeId);
        tokenRepository.insert(initialToken);
        
        // 4. 触发 start 节点的执行
        flowDriver.schedule(instance.getInstanceId(), startNodeId, initialToken);
        
        return instance;
    }
}
```

### 4.2 流程驱动器

流程驱动器是流程执行的"大脑"，职责是：**"收到节点完成事件后，决定下游哪些节点可以执行"**。

```java
@Component
public class FlowDriver {
    
    @Autowired
    private ActivityStateMachine stateMachine;
    
    @Autowired
    private ActivityExecutor activityExecutor;
    
    @Autowired
    private ExpressionEvaluator expressionEvaluator;
    
    /**
     * 节点执行完成后的回调入口
     */
    public void onActivityFinished(StateTransitionResult result) {
        ProcessInstance instance = instanceRepository.load(result.getInstanceId());
        ProcessDefinition def = instance.getDefinition();
        NodeDefinition completedNode = def.getNode(result.getActivityId());
        
        // 1. 查询该节点的所有出边
        List<EdgeDefinition> outgoingEdges = def.getAdjacencyList()
            .getOrDefault(completedNode.getId(), Collections.emptyList());
        
        // 2. 评估每条出边的条件表达式
        for (EdgeDefinition edge : outgoingEdges) {
            boolean shouldFire = evaluateCondition(edge, instance);
            
            if (shouldFire) {
                // 3. 为下游节点创建新的 Token
                Token newToken = new Token(instance.getInstanceId(), edge.getTarget());
                tokenRepository.insert(newToken);
                
                // 4. 驱动下游节点从 PENDING 转为 RUNNING
                schedule(instance.getInstanceId(), edge.getTarget(), newToken);
            }
        }
        
        // 5. 检查流程是否到达终态（所有 Token 都到达 end 节点或被终止）
        checkInstanceCompletion(instance);
    }
    
    /**
     * 将节点放入执行队列
     */
    public void schedule(String instanceId, String nodeId, Token token) {
        // 1. 创建 ActivityInstance（如果尚未创建）
        ActivityInstance activity = activityRepository.find(instanceId, nodeId);
        if (activity == null) {
            activity = new ActivityInstance(instanceId, nodeId);
            activity.setState(ActivityState.PENDING);
            activityRepository.insert(activity);
        }
        
        // 2. 状态机：PENDING → RUNNING
        StateTransitionResult transition = stateMachine.fire(activity, StateEvent.START, null);
        
        // 3. 获取该节点对应的 Behavior
        NodeDefinition nodeDef = token.getInstance().getDefinition().getNode(nodeId);
        ActivityBehavior behavior = nodeRegistry.getBehavior(nodeDef.getType());
        
        // 4. 构建执行上下文
        ExecutionContext ctx = buildContext(token, nodeDef);
        
        // 5. 提交执行器
        activityExecutor.submit(activity, behavior, ctx);
    }
    
    private boolean evaluateCondition(EdgeDefinition edge, ProcessInstance instance) {
        if (edge.getCondition() == null) {
            return true; // 无条件边默认通过
        }
        return expressionEvaluator.evaluate(edge.getCondition(), instance.getVariables());
    }
    
    private void checkInstanceCompletion(ProcessInstance instance) {
        List<Token> activeTokens = tokenRepository.findActiveByInstance(instance.getInstanceId());
        
        // 所有活跃的 Token 都在 end 节点上，则流程完成
        boolean allAtEnd = activeTokens.stream().allMatch(t -> 
            "end".equals(instance.getDefinition().getNode(t.getCurrentNodeId()).getType())
        );
        
        if (allAtEnd) {
            instance.setState(InstanceState.COMPLETED);
            instance.setEndTime(LocalDateTime.now());
            instanceRepository.update(instance);
        }
    }
}
```

### 4.3 执行器与线程模型

流程驱动器决定"执行什么"，执行器负责"怎么执行"。

```java
@Component
public class ActivityExecutor {
    
    // 通用业务执行线程池
    private final ThreadPoolExecutor businessExecutor = new ThreadPoolExecutor(
        4, 32, 60, TimeUnit.SECONDS,
        new LinkedBlockingQueue<>(2000),
        new ThreadFactoryBuilder().setNameFormat("flow-biz-%d").build(),
        new ThreadPoolExecutor.CallerRunsPolicy()
    );
    
    // 长时间等待任务专用线程池（如等 MQ 回执）
    private final ExecutorService waitingExecutor = Executors.newCachedThreadPool(
        new ThreadFactoryBuilder().setNameFormat("flow-wait-%d").build()
    );
    
    /**
     * 提交执行任务
     */
    public void submit(ActivityInstance activity, ActivityBehavior behavior, ExecutionContext ctx) {
        // 判断节点类型，决定执行策略
        if (behavior instanceof WaitingBehavior) {
            // 异步等待型（如 receive 节点），不占用业务线程池
            waitingExecutor.execute(() -> executeWithInterceptors(activity, behavior, ctx));
        } else {
            // 计算型节点，提交到业务线程池
            businessExecutor.execute(() -> executeWithInterceptors(activity, behavior, ctx));
        }
    }
    
    private void executeWithInterceptors(ActivityInstance activity, ActivityBehavior behavior, ExecutionContext ctx) {
        try {
            // 1. 执行前拦截器
            interceptorChain.beforeExecute(ctx);
            
            // 2. 执行业务逻辑
            ExecuteResult result = behavior.execute(ctx);
            
            // 3. 执行后拦截器
            interceptorChain.afterExecute(ctx, result);
            
            // 4. 根据结果驱动状态机
            if (result.isWaiting()) {
                // 异步等待：保持 RUNNING，线程释放，等外部事件回调
                return;
            } else if (result.isSuccess()) {
                stateMachine.fire(activity, StateEvent.COMPLETE, result.getOutput());
            } else {
                stateMachine.fire(activity, StateEvent.FAIL, result.getError());
            }
            
        } catch (Exception e) {
            log.error("节点执行异常", e);
            stateMachine.fire(activity, StateEvent.FAIL, e);
        }
    }
}
```

**线程模型设计决策：**

| 场景 | 线程策略 | 原因 |
|------|---------|------|
| 普通 service 节点 | 业务线程池 | 快速执行，快速释放 |
| receive / deviceCmd 节点 | waiting 线程池 | 可能阻塞数分钟等回执，不能用有限线程池 |
| 定时 / 延迟任务 | 调度线程（Quartz/xxl-job） | 不占用执行线程 |
| 超时检测 | 独立后台线程 + Redis ZSET | 与业务执行完全隔离 |

### 4.4 执行模式与孤儿检测策略

流程引擎同时支持**自动执行**（`AUTO`）和**人工审批**（`MANUAL`）两种节点执行模式。两种模式下，节点的 RUNNING 状态代表完全不同的语义，因此孤儿检测策略必须区分设计。

#### 核心差异

| 维度 | 自动执行（AUTO） | 人工审批（MANUAL） |
|------|----------------|-------------------|
| **RUNNING 含义** | 后台线程正在执行业务逻辑 | 任务已派发给审批人，等待人工操作 |
| **执行线程** | ✅ 有，占用线程池 | ❌ 无，流程挂起 |
| **状态推进方式** | 线程执行完成后自动触发 COMPLETE | 依赖外部调用 `complete(taskId, action)` |
| **孤儿检测依据** | **心跳超时**（线程每 10s 写心跳） | **业务超时**（如 48h 未审批） |
| ** orphan 表现** | 执行线程随服务重启死亡，任务僵死 | 审批人遗漏/离职/通知丢失，任务无人处理 |
| **恢复动作** | 自动重试（幂等）或按超时失败处理 | 催办、升级上级、自动转交 |

#### 节点 Behavior 接口扩展

```java
public interface ActivityBehavior {
    ExecuteResult execute(ExecutionContext ctx);
    
    /**
     * 返回该节点的执行模式，用于流程驱动器决定执行策略
     */
    default ExecutionMode getExecutionMode() {
        return ExecutionMode.AUTO;
    }
}

public enum ExecutionMode {
    AUTO,    // 自动执行：由引擎线程池驱动
    MANUAL   // 人工审批：进入 RUNNING 后挂起，等待外部回调
}
```

#### 人工审批节点的 Behavior 实现

```java
@FlowNodeType("userTask")
public class UserTaskBehavior implements ActivityBehavior {
    
    @Override
    public ExecutionMode getExecutionMode() {
        return ExecutionMode.MANUAL;
    }
    
    @Override
    public ExecuteResult execute(ExecutionContext ctx) {
        // 1. 解析审批人
        String assigneeExpr = ctx.getNodeProperty("assignee");
        String assignee = expressionEvaluator.evaluate(assigneeExpr, ctx.getVariables());
        
        // 2. 创建待办任务（写入 task 表，供前端查询）
        TaskRecord task = new TaskRecord();
        task.setTaskId(generateTaskId());
        task.setInstanceId(ctx.getInstanceId());
        task.setNodeId(ctx.getCurrentNodeId());
        task.setAssignee(assignee);
        task.setState(TaskState.PENDING);
        task.setCreateTime(LocalDateTime.now());
        taskRepository.insert(task);
        
        // 3. 将审批人信息回写到 activity_instance（用于恢复和查询）
        activityRepository.updateAssignee(ctx.getInstanceId(), ctx.getCurrentNodeId(), assignee);
        
        // 4. 返回 WAITING，释放执行线程
        // 流程保持 RUNNING，等审批人调用 complete(taskId, APPROVE/REJECT)
        return ExecuteResult.waiting();
    }
}
```

#### 流程驱动器对两种模式的处理

```java
@Component
public class ActivityExecutor {
    
    public void submit(ActivityInstance activity, ActivityBehavior behavior, ExecutionContext ctx) {
        if (behavior.getExecutionMode() == ExecutionMode.MANUAL) {
            // 人工节点：直接在主线程执行（行为就是写待办任务），然后返回 WAITING
            ExecuteResult result = behavior.execute(ctx);
            // 状态保持 RUNNING，但线程已释放
            // 不再提交到线程池，因为没有后台任务需要跑
        } else {
            // 自动节点：提交到线程池执行
            businessExecutor.execute(() -> executeWithInterceptors(activity, behavior, ctx));
        }
    }
    
    private void executeWithInterceptors(ActivityInstance activity, ActivityBehavior behavior, ExecutionContext ctx) {
        try {
            interceptorChain.beforeExecute(ctx);
            
            // 自动节点执行前启动心跳线程
            HeartbeatRunner heartbeat = null;
            if (behavior.getExecutionMode() == ExecutionMode.AUTO) {
                heartbeat = new HeartbeatRunner(activity.getInstanceId(), activity.getNodeId());
                heartbeat.start();
            }
            
            ExecuteResult result = behavior.execute(ctx);
            
            if (heartbeat != null) {
                heartbeat.stop();
            }
            
            interceptorChain.afterExecute(ctx, result);
            
            // 状态驱动...
            if (result.isWaiting()) {
                return;
            } else if (result.isSuccess()) {
                stateMachine.fire(activity, StateEvent.COMPLETE, result.getOutput());
            } else {
                stateMachine.fire(activity, StateEvent.FAIL, result.getError());
            }
        } catch (Exception e) {
            stateMachine.fire(activity, StateEvent.FAIL, e);
        }
    }
}

/**
 * 自动节点的心跳线程：每 10s 更新 activity 的 heartbeat_time
 */
public class HeartbeatRunner {
    private final String instanceId;
    private final String nodeId;
    private volatile boolean running = true;
    
    public void start() {
        Thread.ofVirtual().start(() -> {
            while (running) {
                activityRepository.updateHeartbeat(instanceId, nodeId, LocalDateTime.now());
                try {
                    Thread.sleep(10_000);
                } catch (InterruptedException e) {
                    Thread.currentThread().interrupt();
                    break;
                }
            }
        });
    }
    
    public void stop() {
        running = false;
    }
}
```

#### 人工节点的超时升级机制

人工审批节点不依赖心跳，而是依赖**业务超时**检测。使用 Redis ZSET 或数据库定时扫描：

```java
@Component
public class ManualTaskTimeoutScanner {
    
    @Autowired
    private ProcessDefinitionRepository definitionRepository;
    
    @Scheduled(fixedDelay = 60000) // 每分钟扫描一次
    public void scan() {
        // 查询所有 MANUAL 模式、RUNNING 状态、且超过 orphanCheck.timeout 的节点
        List<ActivityInstance> timeoutActivities = activityRepository.findManualTimeoutActivities(
            ActivityState.RUNNING,
            ExecutionMode.MANUAL,
            LocalDateTime.now().minusHours(48) // 根据配置动态化
        );
        
        for (ActivityInstance activity : timeoutActivities) {
            // 从数据库实时加载流程定义（支持热更新，不依赖内存缓存）
            ProcessInstance instance = instanceRepository.load(activity.getInstanceId());
            ProcessDefinition def = definitionRepository.load(
                instance.getProcessKey(), instance.getVersion()
            );
            
            // 读取配置的 escalation 策略
            NodeDefinition nodeDef = def.getNode(activity.getNodeId());
            String escalation = nodeDef.getProperties().getString("orphanCheck.escalation");
            
            switch (escalation) {
                case "notifyBoss":
                    // 发送催办/升级通知给上级
                    escalationService.notifyBoss(activity);
                    break;
                case "autoTransfer":
                    // 自动转交给备岗人员
                    escalationService.autoTransfer(activity);
                    break;
                case "timeoutFail":
                    // 按超时失败处理，驱动状态机
                    stateMachine.fire(activity, StateEvent.TIMEOUT, "人工审批超时未处理");
                    break;
            }
        }
    }
}
```

---

## 五、持久化与容错

### 5.1 核心表结构设计

```sql
-- 流程定义版本表
CREATE TABLE flow_definition (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    process_key VARCHAR(64) NOT NULL COMMENT '流程标识',
    version INT NOT NULL COMMENT '版本号',
    name VARCHAR(128) COMMENT '流程名称',
    raw_json TEXT NOT NULL COMMENT '原始LogicFlow JSON',
    status VARCHAR(16) NOT NULL COMMENT 'DRAFT/PUBLISHED/DEPRECATED',
    publish_time DATETIME,
    created_by VARCHAR(64),
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_process_version (process_key, version)
) COMMENT '流程定义';

-- 场景映射表（支持动态配置业务-场景-流程的映射关系）
CREATE TABLE flow_scene_mapping (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    business_code VARCHAR(64) NOT NULL COMMENT '业务编码，如 SECURITY',
    scene_code VARCHAR(64) NOT NULL COMMENT '场景编码，如 FIRE_ALERT',
    process_key VARCHAR(64) NOT NULL COMMENT '映射到的流程标识',
    version INT NOT NULL COMMENT '映射到的版本号',
    status TINYINT DEFAULT 1 COMMENT '0=DISABLED, 1=ENABLED',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_business_scene (business_code, scene_code),
    KEY idx_process_key (process_key, version)
) COMMENT '场景映射';

-- 流程实例表
CREATE TABLE flow_instance (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    instance_id VARCHAR(64) NOT NULL UNIQUE COMMENT '实例ID',
    process_key VARCHAR(64) NOT NULL,
    version INT NOT NULL,
    state VARCHAR(16) NOT NULL COMMENT 'CREATED/RUNNING/COMPLETED/TERMINATED/FAILED',
    variables_json TEXT COMMENT '流程级变量JSON',
    start_time DATETIME NOT NULL,
    end_time DATETIME,
    UNIQUE KEY uk_instance (instance_id),
    KEY idx_process_state (process_key, state, start_time)
) COMMENT '流程实例';

-- 活动实例表（状态机持久化核心）
CREATE TABLE flow_activity (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    instance_id VARCHAR(64) NOT NULL COMMENT '流程实例ID',
    node_id VARCHAR(64) NOT NULL COMMENT '节点定义ID',
    node_type VARCHAR(32) NOT NULL COMMENT '节点类型',
    execution_mode VARCHAR(16) NOT NULL DEFAULT 'AUTO' COMMENT 'AUTO/MANUAL',
    state VARCHAR(16) NOT NULL COMMENT 'PENDING/RUNNING/COMPLETED/FAILED/SKIPPED/CANCELLED',
    assignee VARCHAR(64) COMMENT '人工审批节点的当前处理人',
    input_json TEXT COMMENT '输入参数',
    output_json TEXT COMMENT '输出结果',
    error_msg TEXT COMMENT '异常信息',
    start_time DATETIME,
    end_time DATETIME,
    heartbeat_time DATETIME COMMENT '最后心跳时间（仅 AUTO 节点使用）',
    retry_count INT DEFAULT 0,
    version BIGINT NOT NULL DEFAULT 0 COMMENT '乐观锁版本号',
    UNIQUE KEY uk_instance_node (instance_id, node_id),
    KEY idx_instance_state (instance_id, state),
    KEY idx_state_time (state, start_time),
    KEY idx_heartbeat (state, heartbeat_time)
) COMMENT '活动实例';

-- Token 表（调度核心）
CREATE TABLE flow_token (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    token_id VARCHAR(64) NOT NULL UNIQUE,
    instance_id VARCHAR(64) NOT NULL,
    current_node_id VARCHAR(64) NOT NULL,
    is_active TINYINT(1) DEFAULT 1,
    state VARCHAR(16) COMMENT 'NORMAL',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    KEY idx_instance_active (instance_id, is_active)
) COMMENT 'Token表';

-- 事件日志表（审计与回放）
CREATE TABLE flow_event_log (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    instance_id VARCHAR(64) NOT NULL,
    node_id VARCHAR(64),
    event_type VARCHAR(32) NOT NULL COMMENT 'STATE_CHANGED/ERROR/INTERVENTION',
    from_state VARCHAR(16),
    to_state VARCHAR(16),
    payload_json TEXT,
    created_at DATETIME(3) NOT NULL COMMENT '毫秒级时间戳',
    KEY idx_instance_time (instance_id, created_at)
) COMMENT '事件日志';
```

### 5.2 持久化策略矩阵

| 数据 | 时机 | 方式 | 同步/异步 | 目的 |
|------|------|------|----------|------|
| 流程实例创建 | `start()` 调用时 | INSERT | **同步** | 实例存在性保证 |
| 活动状态变更 | `stateMachine.fire()` 内部 | UPDATE（乐观锁） | **同步** | 服务重启可恢复 |
| 活动输入/输出 | 节点执行完成时 | UPDATE | **同步** | 调试与审计 |
| 流程变量变更 | `setVariable()` 时 | UPDATE instance | **同步** | 下游节点依赖 |
| Token 状态 | 节点完成后移动到下游时 | INSERT/UPDATE | **同步** | 调度正确性 |
| 事件日志 | 每次状态转移 | INSERT | **异步批量** | 审计、对账、回放 |
| 心跳更新（AUTO） | RUNNING 的自动节点每 10s | UPDATE heartbeat_time | **异步** | 自动节点孤儿检测 |
| 审批人/任务状态（MANUAL） | 节点进入 RUNNING 时 | UPDATE assignee + 写入 task 表 | **同步** | 人工节点待办恢复 |

### 5.3 服务重启恢复机制

```java
@Component
public class RecoveryManager {
    
    @Scheduled(fixedDelay = 60000) // 每分钟执行一次兜底扫描
    public void recover() {
        List<String> unfinishedInstances = instanceRepository.findByStates(
            Arrays.asList(InstanceState.RUNNING, InstanceState.CREATED)
        );
        
        for (String instanceId : unfinishedInstances) {
            recoverInstance(instanceId);
        }
    }
    
    @PostConstruct // 启动时立即执行
    public void recoverOnStartup() {
        recover();
    }
    
    @Autowired
    private ProcessDefinitionRepository definitionRepository;
    
    private void recoverInstance(String instanceId) {
        ProcessInstance instance = instanceRepository.load(instanceId);
        // 从数据库实时加载流程定义，不依赖内存缓存
        ProcessDefinition def = definitionRepository.load(
            instance.getProcessKey(), instance.getVersion()
        );
        instance.setDefinition(def);
        
        // 1. 加载该实例下所有非终态的活动
        List<ActivityInstance> unfinishedActivities = activityRepository.findByInstanceAndStates(
            instanceId, Arrays.asList(ActivityState.PENDING, ActivityState.RUNNING)
        );
        
        for (ActivityInstance activity : unfinishedActivities) {
            if (activity.getState() == ActivityState.PENDING) {
                // PENDING：等上游节点完成时自然触发，无需恢复
                log.info("节点 {} 处于 PENDING，等待上游触发", activity.getNodeId());
                
            } else if (activity.getState() == ActivityState.RUNNING) {
                // RUNNING：根据 executionMode 选择恢复策略
                recoverRunningActivity(instance, activity);
            }
        }
        
        // 2. 恢复 Token 状态到流程驱动器
        List<Token> activeTokens = tokenRepository.findActiveByInstance(instanceId);
        for (Token token : activeTokens) {
            flowDriver.restoreToken(instance, token);
        }
    }
    
    private void recoverRunningActivity(ProcessInstance instance, ActivityInstance activity) {
        NodeDefinition nodeDef = instance.getDefinition().getNode(activity.getNodeId());
        ExecutionMode mode = ExecutionMode.valueOf(activity.getExecutionMode());
        
        if (mode == ExecutionMode.AUTO) {
            // ========== 自动节点：心跳检测 ==========
            recoverAutoActivity(instance, activity);
        } else {
            // ========== 人工节点：超时检测 + 待办恢复 ==========
            recoverManualActivity(instance, activity, nodeDef);
        }
    }
    
    /**
     * 自动节点恢复：基于心跳时间判断执行线程是否存活
     */
    private void recoverAutoActivity(ProcessInstance instance, ActivityInstance activity) {
        // 重启后首次检测：如果心跳时间停留在重启前，说明线程已死
        Duration elapsed = Duration.between(activity.getHeartbeatTime(), LocalDateTime.now());
        
        if (elapsed.getSeconds() > 60) {
            log.warn("检测到孤儿任务(自动节点): instance={}, node={}, 已等待 {}s", 
                instance.getInstanceId(), activity.getNodeId(), elapsed.getSeconds());
            
            NodeDefinition nodeDef = instance.getDefinition().getNode(activity.getNodeId());
            String action = nodeDef.getProperties().getString("orphanCheck.action");
            
            if ("retry".equals(action) && activity.getRetryCount() < 3) {
                // 自动重试：重新提交到线程池执行
                log.info("自动重试节点: {}", activity.getNodeId());
                activity.setRetryCount(activity.getRetryCount() + 1);
                activityRepository.update(activity);
                
                Token token = tokenRepository.findByInstanceAndNode(
                    instance.getInstanceId(), activity.getNodeId()
                );
                flowDriver.schedule(instance.getInstanceId(), activity.getNodeId(), token);
            } else {
                // 按超时失败处理
                stateMachine.fire(activity, StateEvent.TIMEOUT, 
                    "服务重启后检测到孤儿任务，按超时处理");
            }
        } else {
            log.info("自动节点 {} 心跳正常，线程存活", activity.getNodeId());
        }
    }
    
    /**
     * 人工节点恢复：检查待办任务是否存在，无需心跳
     */
    private void recoverManualActivity(ProcessInstance instance, ActivityInstance activity, 
                                       NodeDefinition nodeDef) {
        // 人工节点没有执行线程，不存在"线程死亡"的问题
        // 需要恢复的是：待办任务是否还存在（可能 task 表数据丢失）
        
        TaskRecord task = taskRepository.findByInstanceAndNode(
            instance.getInstanceId(), activity.getNodeId()
        );
        
        if (task == null) {
            // 极端情况：activity 是 RUNNING，但 task 表记录丢失
            // 根据配置决定：重新生成待办 或 按超时处理
            log.warn("人工节点待办任务丢失: instance={}, node={}", 
                instance.getInstanceId(), activity.getNodeId());
            
            // 重新生成待办任务
            String assignee = activity.getAssignee();
            if (assignee == null) {
                assignee = resolveAssignee(nodeDef, instance);
            }
            
            TaskRecord newTask = new TaskRecord();
            newTask.setTaskId(generateTaskId());
            newTask.setInstanceId(instance.getInstanceId());
            newTask.setNodeId(activity.getNodeId());
            newTask.setAssignee(assignee);
            newTask.setState(TaskState.PENDING);
            taskRepository.insert(newTask);
            
            log.info("已重新生成待办任务: taskId={}, assignee={}", newTask.getTaskId(), assignee);
        } else {
            log.info("人工节点 {} 待办任务正常: taskId={}, assignee={}", 
                activity.getNodeId(), task.getTaskId(), task.getAssignee());
        }
        
        // 人工节点的超时检测由独立定时任务（ManualTaskTimeoutScanner）负责，
        // 恢复流程不处理超时，只保证待办任务可查询
    }
    
    private String resolveAssignee(NodeDefinition nodeDef, ProcessInstance instance) {
        String assigneeExpr = nodeDef.getProperties().getString("assignee");
        return expressionEvaluator.evaluate(assigneeExpr, instance.getVariables());
    }
}
```

### 5.4 事件溯源（Event Sourcing）与回放

`flow_event_log` 表采用**事件溯源**思想记录每一次状态变更：

```sql
-- 查询某实例的完整执行轨迹
SELECT * FROM flow_event_log 
WHERE instance_id = 'ins_xxx' 
ORDER BY created_at ASC;
```

**回放能力：**

```java
public void replay(String instanceId) {
    List<EventLog> events = eventLogRepository.findByInstanceOrderByTime(instanceId);
    
    for (EventLog event : events) {
        if ("STATE_CHANGED".equals(event.getEventType())) {
            System.out.printf("[%s] %s: %s → %s%n", 
                event.getCreatedAt(), 
                event.getNodeId(),
                event.getFromState(),
                event.getToState()
            );
        }
    }
}
```

**用途：**
- 线上问题排查：精确还原执行路径
- 客户审计：提供不可抵赖的执行证据
- 对账：与外部系统（如设备网关）核对指令下发记录

---

## 六、应急领域特化

### 6.1 超时升级链（Escalation）

#### 设计

超时升级不是简单的"超时即失败"，而是**多级通知强度递增**。

#### 配置

```json
{
  "id": "wait_response",
  "type": "receive",
  "properties": {
    "timeout": 300,
    "escalation": [
      { "delay": 300, "action": "SMS", "template": "ESC_L1", "targets": ["${leaderPhone}"] },
      { "delay": 600, "action": "PHONE_CALL", "template": "ESC_L2", "targets": ["${leaderPhone}", "${backupPhone}"] },
      { "delay": 900, "action": "BROADCAST", "template": "ESC_L3", "targets": ["@all"] }
    ]
  }
}
```

#### 实现

```java
@Component
public class EscalationManager {
    
    @Autowired
    private StringRedisTemplate redisTemplate;
    
    @Autowired
    private NotificationService notificationService;
    
    private static final String ESCALATION_KEY_PREFIX = "escalation:";
    
    /**
     * 节点进入 RUNNING 时调用
     */
    public void onActivityStarted(ActivityInstance activity) {
        EscalationConfig config = parseEscalationConfig(activity);
        if (config == null || config.getLevels().isEmpty()) {
            return;
        }
        
        for (EscalationLevel level : config.getLevels()) {
            String taskKey = buildKey(activity, level);
            long executeAt = System.currentTimeMillis() + level.getDelay() * 1000;
            
            // 使用 Redis ZSET 作为分布式延迟队列
            redisTemplate.opsForZSet().add(
                "escalation:queue", 
                taskKey, 
                executeAt
            );
        }
        
        // 同时记录该节点的升级任务列表，用于完成时批量取消
        redisTemplate.opsForSet().add(
            ESCALATION_KEY_PREFIX + activity.getInstanceId() + ":" + activity.getNodeId(),
            config.getLevels().stream().map(this::buildKey).toArray(String[]::new)
        );
    }
    
    /**
     * 节点到达终态时调用，取消所有待执行的升级任务
     */
    public void onActivityFinished(ActivityInstance activity) {
        String setKey = ESCALATION_KEY_PREFIX + activity.getInstanceId() + ":" + activity.getNodeId();
        Set<String> taskKeys = redisTemplate.opsForSet().members(setKey);
        
        if (taskKeys != null && !taskKeys.isEmpty()) {
            // 从延迟队列中移除
            redisTemplate.opsForZSet().remove("escalation:queue", taskKeys.toArray());
            // 删除记录集合
            redisTemplate.delete(setKey);
        }
    }
    
    /**
     * 定时拉取到期任务（独立后台线程或 xxl-job）
     */
    @Scheduled(fixedRate = 5000)
    public void processDueEscalations() {
        long now = System.currentTimeMillis();
        Set<String> dueTasks = redisTemplate.opsForZSet()
            .rangeByScore("escalation:queue", 0, now);
        
        for (String taskKey : dueTasks) {
            EscalationTask task = parseTaskKey(taskKey);
            
            // 双重检查：节点是否仍在 RUNNING
            ActivityInstance activity = activityRepository.find(task.getInstanceId(), task.getNodeId());
            if (activity != null && activity.getState() == ActivityState.RUNNING) {
                notificationService.send(task.getAction(), task.getTemplate(), task.getTargets());
                
                // 如果是最后一级升级，触发超时失败
                if (task.isLastLevel()) {
                    stateMachine.fire(activity, StateEvent.TIMEOUT, 
                        "升级链全部触发仍未收到响应");
                }
            }
            
            // 从队列移除（无论是否执行，避免重复）
            redisTemplate.opsForZSet().remove("escalation:queue", taskKey);
        }
    }
}
```

**关键点：**

1. **取消机制必须可靠**：节点完成时，所有后续升级任务必须从延迟队列中移除，否则会造成误通知
2. **双重检查**：拉取到期任务后，再次查库确认节点仍在 RUNNING，防止"刚好完成但尚未取消"的竞态
3. **分布式**：Redis ZSET 保证集群多实例下只有一个实例处理同一任务（利用原子操作）

### 6.2 可靠指令投递（Reliable Command Delivery）

#### 时序与状态

```
[流程引擎] --1.写本地事务表(SENT)--> [MySQL]
         --2.发送MQ指令--> [RocketMQ]
         --3.MQ Confirm--> [流程引擎] 更新事务表为 DELIVERED
         
[设备网关] <--4.消费MQ-- [RocketMQ]
         --5.下发设备--> [终端设备]
         <--6.设备回执-- [终端设备]
         --7.发送回执MQ--> [RocketMQ]
         
[流程引擎] <--8.消费回执-- [RocketMQ]
         --9.更新事务表(ACKED)--> [MySQL]
         --10.驱动状态机 COMPLETED-->
```

#### 本地事务表

```sql
CREATE TABLE flow_command_tx (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    command_id VARCHAR(64) NOT NULL UNIQUE,
    instance_id VARCHAR(64) NOT NULL,
    node_id VARCHAR(64) NOT NULL,
    device_id VARCHAR(64) NOT NULL,
    command_payload TEXT,
    state VARCHAR(16) NOT NULL COMMENT 'SENT/DELIVERED/ACKED/FAILED',
    send_time DATETIME NOT NULL,
    ack_time DATETIME,
    retry_count INT DEFAULT 0,
    max_retry INT DEFAULT 3,
    error_msg TEXT,
    UNIQUE KEY uk_command (command_id),
    KEY idx_instance (instance_id, node_id),
    KEY idx_state_time (state, send_time)
);
```

#### 实现

```java
@Component
public class DeviceCommandBehavior implements ActivityBehavior {
    
    @Autowired
    private CommandTxRepository txRepository;
    
    @Autowired
    private RocketMQTemplate mqTemplate;
    
    @Override
    public ExecuteResult execute(ExecutionContext ctx) {
        String commandId = generateCommandId(ctx);
        String deviceId = ctx.getVariable("deviceId");
        
        // 1. 幂等检查：该节点是否已发送过指令
        CommandTx existing = txRepository.findByCommandId(commandId);
        if (existing != null) {
            if (existing.getState() == TxState.ACKED) {
                return ExecuteResult.success(existing.getAckPayload());
            }
            // 否则继续等待或重试
            return ExecuteResult.waiting();
        }
        
        // 2. 写本地事务表（SENT 状态）
        CommandTx tx = new CommandTx();
        tx.setCommandId(commandId);
        tx.setInstanceId(ctx.getInstanceId());
        tx.setNodeId(ctx.getCurrentActivityId());
        tx.setDeviceId(deviceId);
        tx.setCommandPayload(ctx.getVariable("cmdPayload"));
        tx.setState(TxState.SENT);
        tx.setSendTime(LocalDateTime.now());
        txRepository.insert(tx);
        
        // 3. 发送 MQ（异步 Confirm）
        mqTemplate.asyncSend("device-command-topic", 
            new DeviceCommand(commandId, deviceId, tx.getCommandPayload()),
            new SendCallback() {
                @Override
                public void onSuccess(SendResult sendResult) {
                    txRepository.updateState(commandId, TxState.DELIVERED);
                }
                @Override
                public void onException(Throwable e) {
                    log.error("指令发送失败: {}", commandId, e);
                    // 定时任务会扫描 SENT 状态超时的记录进行重试
                }
            }
        );
        
        // 4. 返回 WAITING，释放执行线程
        return ExecuteResult.waiting();
    }
}

// MQ 回执监听器
@Component
@RocketMQMessageListener(topic = "device-response-topic", consumerGroup = "flow-engine")
public class DeviceResponseListener implements RocketMQListener<DeviceResponse> {
    
    @Autowired
    private CommandTxRepository txRepository;
    
    @Autowired
    private ActivityStateMachine stateMachine;
    
    @Override
    public void onMessage(DeviceResponse response) {
        CommandTx tx = txRepository.findByCommandId(response.getCommandId());
        if (tx == null || tx.getState() == TxState.ACKED) {
            return; // 幂等：已处理则丢弃
        }
        
        if (response.isSuccess()) {
            tx.setState(TxState.ACKED);
            tx.setAckTime(LocalDateTime.now());
            tx.setAckPayload(response.getPayload());
            txRepository.update(tx);
            
            // 驱动流程继续
            ActivityInstance activity = activityRepository.find(tx.getInstanceId(), tx.getNodeId());
            stateMachine.fire(activity, StateEvent.COMPLETE, response.getPayload());
        } else {
            // 设备执行失败，进入重试或失败分支
            handleDeviceFailure(tx, response);
        }
    }
}
```

**兜底重试（定时任务）：**

```java
@Scheduled(fixedRate = 30000)
public void retryTimeoutCommands() {
    // 查询 SENT 状态超过 60s 未确认的记录
    List<CommandTx> timeoutCommands = txRepository.findByStateAndSendTimeBefore(
        TxState.SENT, LocalDateTime.now().minusSeconds(60)
    );
    
    for (CommandTx tx : timeoutCommands) {
        if (tx.getRetryCount() >= tx.getMaxRetry()) {
            // 超过最大重试，标记失败，触发状态机 FAIL
            tx.setState(TxState.FAILED);
            txRepository.update(tx);
            
            ActivityInstance activity = activityRepository.find(tx.getInstanceId(), tx.getNodeId());
            stateMachine.fire(activity, StateEvent.FAIL, "设备指令超时未响应");
        } else {
            // 重试
            tx.setRetryCount(tx.getRetryCount() + 1);
            txRepository.update(tx);
            
            mqTemplate.syncSend("device-command-topic", 
                new DeviceCommand(tx.getCommandId(), tx.getDeviceId(), tx.getCommandPayload()));
        }
    }
}
```

### 6.3 动态拦截器链

```java
public interface ActivityInterceptor {
    int getOrder(); // 优先级，越小越先执行
    void beforeExecute(ExecutionContext ctx);
    void afterExecute(ExecutionContext ctx, ExecuteResult result);
}

@Component
public class InterceptorChain {
    
    private final List<ActivityInterceptor> interceptors;
    
    public InterceptorChain(List<ActivityInterceptor> interceptors) {
        this.interceptors = interceptors.stream()
            .sorted(Comparator.comparingInt(ActivityInterceptor::getOrder))
            .collect(Collectors.toList());
    }
    
    public void beforeExecute(ExecutionContext ctx) {
        for (ActivityInterceptor interceptor : interceptors) {
            interceptor.beforeExecute(ctx);
        }
    }
    
    public void afterExecute(ExecutionContext ctx, ExecuteResult result) {
        // 反向执行，类似 Servlet Filter 的 unwind
        for (int i = interceptors.size() - 1; i >= 0; i--) {
            interceptors.get(i).afterExecute(ctx, result);
        }
    }
}
```

**内置拦截器：**

```java
// 1. 权限拦截器
@Component
@Order(10)
public class AuthInterceptor implements ActivityInterceptor {
    @Override
    public void beforeExecute(ExecutionContext ctx) {
        String requiredRole = ctx.getNodeDefinition().getProperties().getString("requiredRole");
        if (requiredRole != null) {
            String operatorRole = ctx.getVariable("operatorRole");
            if (!requiredRole.equals(operatorRole)) {
                throw new AccessDeniedException("无权执行该节点: " + requiredRole);
            }
        }
    }
}

// 2. 审计拦截器
@Component
@Order(20)
public class AuditInterceptor implements ActivityInterceptor {
    @Autowired
    private AuditLogRepository auditRepository;
    
    @Override
    public void beforeExecute(ExecutionContext ctx) {
        ctx.setAttribute("auditStartTime", System.nanoTime());
    }
    
    @Override
    public void afterExecute(ExecutionContext ctx, ExecuteResult result) {
        long duration = System.nanoTime() - (Long) ctx.getAttribute("auditStartTime");
        auditRepository.save(new AuditLog(
            ctx.getInstanceId(),
            ctx.getCurrentActivityId(),
            ctx.getVariable("operatorId"),
            result.isSuccess() ? "SUCCESS" : "FAIL",
            duration / 1_000_000 // ms
        ));
    }
}

// 3. 熔断拦截器（针对外部 HTTP 调用节点）
@Component
@Order(30)
public class CircuitBreakerInterceptor implements ActivityInterceptor {
    private final Map<String, CircuitBreaker> breakers = new ConcurrentHashMap<>();
    
    @Override
    public void beforeExecute(ExecutionContext ctx) {
        if (isExternalHttpNode(ctx)) {
            CircuitBreaker cb = breakers.computeIfAbsent(
                ctx.getCurrentActivityId(), 
                k -> new CircuitBreaker(5, 60000) // 5次失败，熔断60s
            );
            if (cb.isOpen()) {
                throw new CircuitBreakerOpenException("节点熔断中，请稍后重试");
            }
        }
    }
    
    @Override
    public void afterExecute(ExecutionContext ctx, ExecuteResult result) {
        if (isExternalHttpNode(ctx) && !result.isSuccess()) {
            breakers.get(ctx.getCurrentActivityId()).recordFailure();
        }
    }
}
```

---

## 七、扩展机制

### 7.1 节点类型注册（Spring SPI + 注解扫描）

```java
@Target(ElementType.TYPE)
@Retention(RetentionPolicy.RUNTIME)
@Component
public @interface FlowNodeType {
    String value(); // 节点类型标识，如 "notifySms", "gisDispatch"
}

// 业务方实现
@FlowNodeType("gisDispatch")
public class GisDispatchBehavior implements ActivityBehavior {
    @Autowired
    private GisService gisService;
    
    @Override
    public ExecuteResult execute(ExecutionContext ctx) {
        LatLng target = ctx.getVariable("targetLocation");
        List<Team> teams = gisService.findNearest(target, 3);
        ctx.setVariable("assignedTeams", teams);
        return ExecuteResult.success();
    }
}
```

```java
@Component
public class NodeTypeRegistry implements ApplicationContextAware {
    private final Map<String, ActivityBehavior> behaviors = new HashMap<>();
    
    @Override
    public void setApplicationContext(ApplicationContext ctx) {
        Map<String, Object> beans = ctx.getBeansWithAnnotation(FlowNodeType.class);
        for (Object bean : beans.values()) {
            FlowNodeType anno = bean.getClass().getAnnotation(FlowNodeType.class);
            behaviors.put(anno.value(), (ActivityBehavior) bean);
        }
    }
    
    public ActivityBehavior getBehavior(String type) {
        ActivityBehavior b = behaviors.get(type);
        if (b == null) throw new UnsupportedNodeTypeException(type);
        return b;
    }
    
    public boolean isRegistered(String type) {
        return behaviors.containsKey(type);
    }
}
```

### 7.2 自定义条件表达式函数

```java
@Component
public class FlowExpressionFunctions {
    
    @FunctionMapping("allSuccess")
    public boolean allSuccess(ExecutionContext ctx) {
        // 获取当前流程实例中所有已完成的节点
        List<ActivityInstance> activities = activityRepository.findByInstanceId(
            ctx.getInstanceId()
        );
        return activities.stream().allMatch(a -> a.getState() == ActivityState.COMPLETED);
    }
    
    @FunctionMapping("hasTimeout")
    public boolean hasTimeout(ExecutionContext ctx) {
        List<ActivityInstance> activities = activityRepository.findByInstanceId(
            ctx.getInstanceId()
        );
        return activities.stream().anyMatch(a -> a.getState() == ActivityState.FAILED 
            && "TIMEOUT".equals(a.getErrorCode()));
    }
}
```

### 7.3 流程定义管理 API

支持运行时动态发布、查询和绑定流程定义，无需重启服务。

```java
@RestController
@RequestMapping("/api/flow-definition")
public class FlowDefinitionController {
    
    @Autowired
    private ProcessDefinitionRepository definitionRepository;
    
    @Autowired
    private SceneMappingRepository sceneMappingRepository;
    
    /**
     * 发布流程定义（编辑保存）
     */
    @PostMapping
    public Result<String> publish(@RequestBody FlowDefinitionDTO dto) {
        // 1. 编译校验
        CompiledProcessDefinition compiled = compiler.compile(dto.getRawJson());
        
        // 2. 生成新版本号
        int newVersion = definitionRepository.getNextVersion(dto.getProcessKey());
        
        // 3. 存入数据库（初始状态为 DRAFT）
        definitionRepository.save(dto.getProcessKey(), newVersion, dto.getRawJson(), Status.DRAFT);
        
        return Result.ok(dto.getProcessKey() + ":" + newVersion);
    }
    
    /**
     * 发布上线（DRAFT → PUBLISHED）
     */
    @PutMapping("/{processKey}/{version}/publish")
    public Result<Void> publishOnline(@PathVariable String processKey, 
                                       @PathVariable Integer version) {
        definitionRepository.updateStatus(processKey, version, Status.PUBLISHED);
        // 可选：失效本地缓存
        definitionCache.invalidate(processKey, version);
        return Result.ok();
    }
    
    /**
     * 配置场景映射（动态绑定业务场景与流程）
     */
    @PostMapping("/scene-mapping")
    public Result<Void> bindScene(@RequestBody SceneMappingDTO dto) {
        sceneMappingRepository.saveOrUpdate(
            dto.getBusinessCode(),
            dto.getSceneCode(),
            dto.getProcessKey(),
            dto.getVersion()
        );
        return Result.ok();
    }
    
    /**
     * 查询业务下的所有场景映射
     */
    @GetMapping("/scenes")
    public Result<List<SceneMappingVO>> listScenes(
            @RequestParam String businessCode) {
        return Result.ok(sceneMappingRepository.findByBusiness(businessCode));
    }
}
```

**动态发布的安全保障：**

| 检查项 | 说明 |
|--------|------|
| 编译时校验 | 发布前必须通过 DAG 编译器的全部校验（环检测、孤立节点、条件表达式语法等） |
| 状态隔离 | DRAFT 版本不可被流程实例引用，只有 PUBLISHED 才能启动 |
| 实例绑定 | 已运行的实例绑定到启动时的 version，不受后续发布影响 |
| 灰度切换 | 通过修改 `flow_scene_mapping` 指向新版本，新实例即时生效，老实例不受影响 |

---

## 八、性能与优化

### 8.1 内存模型

| 数据 | 存储位置 | 策略 | 说明 |
|------|---------|------|------|
| 流程定义 (`ProcessDefinition`) | 本地内存（可选二级缓存）+ MySQL | 运行时从数据库加载并编译 | 不可变对象，线程安全 |
| 运行中实例变量 | Redis Hash | 按 instance_id 分 key | 避免频繁查 MySQL |
| 活动状态 | MySQL（主）+ Redis（辅） | 状态变更双写 | Redis 用于快速查询，MySQL 用于持久化 |
| Token 状态 | MySQL + 本地缓存 | 运行时加载到流程驱动器内存 | 流程驱动器单例持有活跃 Token |

### 8.2 数据库优化

```sql
-- 活动表按 instance_id 分库分表（如 128 张表）
-- 分片键：instance_id 的 hash % 128

-- 历史归档：
-- 已完成超过 30 天的实例，迁移到 flow_instance_history / flow_activity_history
-- 原表只保留近期数据，保证查询性能
```

### 8.3 异步与批量

```java
// 事件日志批量写入（每 100 条或每 1s 刷盘一次）
@Component
public class EventLogBuffer {
    private final List<EventLog> buffer = new ArrayList<>();
    
    public void append(EventLog log) {
        synchronized (buffer) {
            buffer.add(log);
            if (buffer.size() >= 100) {
                flush();
            }
        }
    }
    
    @Scheduled(fixedRate = 1000)
    public void scheduledFlush() {
        synchronized (buffer) {
            if (!buffer.isEmpty()) {
                flush();
            }
        }
    }
    
    private void flush() {
        eventLogRepository.batchInsert(new ArrayList<>(buffer));
        buffer.clear();
    }
}
```

---

## 九、面试速查

### Q1：为什么自研，不用 Flowable？

> 应急通信是"指令调度流"而非"人工审批流"，核心特征是超时升级链、GIS 联动、设备可靠投递。Flowable 的 BPMN 模型过重（历史表、变量表、事件日志表），且这些领域概念无法直接映射到 BPMN 规范。我们选择**轻量级 JSON DSL + 领域专用执行引擎**，用 1/10 的代码量实现了应急场景所需的全部语义，且性能更优。

### Q2：如何保证服务重启不丢状态？

> 三个机制：① **活动级快照**：每个节点状态变更同步写 MySQL（乐观锁保证并发安全）；② **Token 持久化**：节点完成后移动到下游时写库，重启后恢复 Token 位置；③ **孤儿检测**：按节点执行模式区分策略——**自动节点**（`AUTO`）由后台线程每 10s 写心跳，重启后心跳超时则判定为孤儿，支持自动重试或按超时失败处理；**人工节点**（`MANUAL`）不依赖心跳，而是按业务超时（如 48h 未审批）检测，超时后触发催办、升级或自动转交。

### Q3：引擎支持多大并发？

> 流程驱动器是**单线程无锁**的，只负责驱动 DAG 拓扑和状态流转；业务逻辑提交到线程池异步执行。单机支撑 **5000+ TPS** 的流程事件，瓶颈通常在业务侧的 HTTP 调用或 DB。水平扩展时按 `instance_id` 哈希分片，同一实例路由到同一节点，保证状态机串行化。

### Q4：升级链的定时任务如果挂了怎么办？

> ① 使用 **Redis ZSET** 做分布式延迟队列，多实例部署时只有一个实例能拿到到期任务（`ZREVRANGEBYSCORE + ZREM` 的原子性保证）；② 任务执行前**双重检查**节点是否仍在 RUNNING，防止误触发；③ 节点完成时**批量取消**后续升级任务，取消操作是幂等的。

### Q5：设备指令如果设备永远不回执怎么办？

> 三层兜底：① **MQ Confirm**：确保消息到达 Broker；② **本地事务表 + 定时重试**：`SENT` 状态超过 60s 未 `ACKED` 则自动重试，最多 3 次；③ **流程级超时**：重试耗尽后状态机触发 `FAIL`，流程进入预定义的降级分支（如人工介入节点）。

---

> **核心设计哲学**：编译时做尽所有校验，运行时保持极简，故障时快速恢复。所有复杂性（表达式解析、条件判断、并发控制）都推给编译期和基础设施，执行引擎本身只负责"收到事件 → 查表转移 → 驱动下游"。
